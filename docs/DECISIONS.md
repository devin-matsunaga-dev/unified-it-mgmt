# DECISIONS.md — Decision Log

> One line per settled choice: "chose X over Y because Z (WP-N.N, date)". Appended by Codex at the end of every session; never relitigated without a work package.

- Bootstrap: modular monolith over microservices; extract Python pollers/ML only (see ARCHITECTURE.md §9)
- Bootstrap: platform versions pinned to LTS/latest-supported — .NET 10, Aspire 13.x, Node 24, Python 3.13+ (see WORKFLOW.md table)
- Bootstrap: UI = Tailwind + shadcn/ui + lucide per DESIGN.md reference screenshot
- WP-0.1: no new architectural decisions; the skeleton follows the existing steering documents (2026-08-06)
- WP-0.2: no new architectural decisions; infrastructure follows the resource topology already settled in ARCHITECTURE.md (2026-08-06)
- WP-0.3: no new architectural decisions; bearer authentication, policy authorization, and provider-neutral configuration follow the existing authentication architecture (2026-08-06)
- WP-0.4: no new architectural decisions; append-only auditing, notification logging, and scheduled jobs follow the existing platform-service architecture (2026-08-06)
- WP-0.5: chose MassTransit 8.5.10 over 9.x because 9.x requires a commercial runtime license; EF transactional outbox and deterministic consumer deduplication remain unchanged (2026-08-06)
- WP-0.6: chose oidc-client-ts with authorization-code PKCE and automatic silent renewal because it keeps the SPA provider-neutral and stores tokens only in session storage (2026-08-06)
- WP-0.7: chose GitHub Actions with GHCR tag publishing because the repository is hosted on GitHub and can authenticate image pushes without additional credentials (2026-08-06)
- WP-0.8: no new architectural decisions; the console seeder and realm-import users follow the existing Platform and Keycloak development topology (2026-08-06)
- WP-1.1: no new architectural decisions; ticket storage, auditing, and event publication follow the existing module-schema and Platform outbox architecture (2026-08-06)
- WP-1.2: no new architectural decisions; persisted workflow configuration, guarded transitions, history, auditing, and outbox publication follow the existing Helpdesk and Platform patterns (2026-08-06)
- WP-1.3: no new architectural decisions; team-owned queues, persisted round-robin state, assignment history, auditing, and technician-scoped queries follow the existing Helpdesk patterns (2026-08-06)
- WP-1.4: chose a 25 MB attachment ceiling with an explicit document/image/archive extension and media-type allowlist because the required 10 MB upload needs headroom while executable and unknown formats must fail closed (2026-08-06)
- WP-1.4: chose private MinIO objects served through authorized API downloads because ticket visibility must be enforced for every attachment access (2026-08-06)
- WP-1.5: chose persisted elapsed business seconds plus an active interval over wall-clock deadlines because Pending pauses and calendar changes must not consume SLA time (2026-08-07)
- WP-1.5: chose a per-policy warning percentage constrained to 1–99 because different service tiers need configurable lead time before breach (2026-08-07)
- WP-1.5: chose priority-only matching for currently uncategorized tickets while persisting an optional category selector because ticket categories are introduced by WP-1.9 (2026-08-07)
- WP-1.5: chose resolution-breach reassignment to the next ordered queue technician because it extends the established round-robin ownership model without adding a second escalation topology (2026-08-07)
- WP-1.6: chose MailKit 4.17.0 for IMAP, MIME parsing, and SMTP because it provides maintained protocol support and passes the repository vulnerability gate (2026-08-07)
- WP-1.6: chose unique RFC Message-ID persistence with stable ticket Message-ID headers and subject-token fallback because retries must be idempotent and replies must thread across mail clients (2026-08-07)
- WP-1.6: chose MailHog for outbound capture plus GreenMail for the development IMAP inbox because MailHog does not provide IMAP retrieval (2026-08-07)
- WP-1.7: chose an idempotently seeded Service Desk queue with development technician usernames because agent assignment must be verifiable in a fresh Aspire environment without manual API setup (2026-08-07)
- WP-1.7: chose to persist a comment-author display-name snapshot alongside the immutable identity ID because timelines need human-readable attribution without coupling Helpdesk reads to an identity provider (2026-08-07)
- WP-1.7: chose the same display-name snapshot pattern for ticket requesters because ticket headers need readable attribution while ownership checks continue using immutable identity IDs (2026-08-07)
- WP-1.7: chose to snapshot the requester email separately from the identity ID because lifecycle notifications require a stable SMTP recipient and must never treat an OIDC subject as an address (2026-08-07)
- WP-1.7: chose to email only public comments authored by agents because internal notes must remain private and EndUser/inbound-email comments must not create mail loops (2026-08-07)
- WP-1.8: chose a separate `/portal` route tree with its own top-nav shell restricted to the EndUser role over reusing the agent AppShell because requester scoping only exists for EndUsers and an agent inside the portal would see every ticket as "mine" (2026-08-07)
- WP-1.8: chose to render the portal submit form's request picker from the existing Incident/ServiceRequest type rather than adding a ticket category column because categories are WP-1.9's scope and a flat interim string would need rework (2026-08-07)
- WP-1.8: chose to restrict EndUser transitions server-side to Resolved→Closed (403 ProblemDetails otherwise) because close-confirm must be enforced below the UI, not by hiding buttons (2026-08-07)
- WP-1.8: chose to route portal submissions to the seeded Service Desk queue by name, falling back to the first queue then unqueued, because self-service tickets must enter round-robin assignment without asking requesters to pick a queue (2026-08-07)
- WP-1.9: chose custom field definitions that apply only to the category they are attached to, with no inheritance from parent categories, because the WP specifies per-category fields and inheritance would make required-field resolution depend on tree traversal at every write (2026-08-07)
- WP-1.9: chose one row per ticket/field value with a canonical string `value` column over a JSON blob because uniqueness, deletion when a category changes, and future reporting all need the value addressable by field id (2026-08-07)
- WP-1.9: chose to canonicalise submitted values by declared type (decimal invariant, `yyyy-MM-dd`, exact select option) at the API edge because storage must not keep locale-dependent or unlisted values (2026-08-07)
- WP-1.9: chose a maximum category nesting depth of 3 with explicit cycle detection on reparent because deeper trees are unusable in a single-select picker and cycles would make the tree query non-terminating (2026-08-07)
- WP-1.9: chose to leave SLA policy matching on its existing free-text `Category` string and priority-only lookup because rewiring SLA to the new category tree is outside this WP's text (2026-08-07)
- WP-1.9: chose to keep the portal's Incident/ServiceRequest choice as a compact select in the details card rather than deriving ticket type from the category because no category→type mapping exists and requesters distinguish "broken" from "need something" usefully (2026-08-07)
- WP-1.9: chose to block deleting a category that has children or tickets (409) and to offer deactivation instead because ticket history must keep resolving its category name (2026-08-07)
- WP-1.9: chose to always refetch the category tree on mount (`staleTime: 0`) rather than share the 30s default because a cached tree can omit a field an admin has since made required, producing a 400 the form cannot attribute to any input (2026-08-07)
- WP-1.9: chose to keep the dev Postgres volume unnamed (fresh database whenever AppHost config changes) over pinning a stable volume name because the project wants a clean slate while testing; the consequence is that every fixture needed for manual verification must be seeded, not hand-created (2026-08-07)
- WP-1.9: chose to seed a required "Asset tag" custom field on the seeded "Laptop or desktop" category because a clean-slate database must still demonstrate a required per-category field without manual API setup (2026-08-07)
- WP-1.9: chose to seed a ten-node starter category tree in the demo seeder because a fresh Aspire environment must open the ticket forms with a usable picker, following the WP-1.7 seeded-queue precedent (2026-08-07)
- WP-1.10: chose database-generated `tsvector` columns (english) on `tickets(title, description)` and `ticket_comments(body)` with GIN indexes over query-time `to_tsvector` because only a stored column can be indexed, and generation keeps the vector in step with edits without trigger code (2026-08-07)
- WP-1.10: chose AND-ed prefix tsqueries (`auro:*`) built in application code over `websearch_to_tsquery` because the search box has to narrow results while an agent is still typing, and whole-lexeme matching only found the completed word (2026-08-07)
- WP-1.10: chose to strip tsquery operator characters from the typed term rather than escape them because the box is a search field, not a query language, and `to_tsquery` must never raise a parse error (2026-08-07)
- WP-1.10: chose to OR the ticket sequence number into the search predicate because agents paste "INC-000042" into the box and the number is a computed property that no text index covers (2026-08-07)
- WP-1.10: chose to search tickets and their comments in one query (ticket vector OR any matching comment vector), ranking by the ticket vector, because the WP requires a term inside an old comment to surface its ticket in the normal list (2026-08-07)
- WP-1.10: chose to exclude internal notes from requester searches while including them for agents because a requester must not learn that a private note exists (2026-08-07)
- WP-1.10: chose to add real server-side list filters (status, priority, type, queue, assignee/unassigned, category) to `/api/tickets` because a saved view is exactly a persisted filter set and the list previously accepted only pagination (2026-08-07)
- WP-1.10: chose to persist a saved view's filter as one `jsonb` column holding the same DTO the list endpoint accepts, over a column per filter member, because the two must not drift and new filters must not need a migration (2026-08-07)
- WP-1.10: chose shared views visible to every agent but editable and deletable only by their owner (403 otherwise) because users have no team membership anywhere in the model to scope sharing by (2026-08-07)
- WP-1.10: chose to enforce the agent-only surfaces (views, canned responses) inside the services with 403 ProblemDetails rather than adding an authorization policy because `CanManageTickets` deliberately includes EndUser and auth configuration is outside this WP (2026-08-07)
- WP-1.10: chose a fixed canned-response placeholder allowlist (`ticket.id`, `ticket.number`, `ticket.title`, `requester.name`, `agent.name`) with unknown tokens left literal because a silent empty substitution would ship a broken reply to a customer (2026-08-07)
- WP-1.10: chose to render canned responses server-side against the ticket rather than substituting in the browser because the requester display name is a Helpdesk snapshot and the same rendering will be reused by future automation (2026-08-07)
- WP-1.10: chose to have a canned response replace the previously inserted one only while the reply box still ends with it, appending once the agent has edited or typed past it, because cycling through templates must not leave stacked copies but hand-written text must never be destroyed (2026-08-07)
- WP-1.10: chose to seed three canned responses and one shared view because the dev database is recreated on most AppHost restarts and both features need a fixture to be verifiable by hand (2026-08-07)
- WP-1.11: chose a separate `HelpdeskHistorySeeder` over extending `HelpdeskDemoDataSeeder` because reference data and generated history have different result shapes, and the reference seeder must stay readable as the list of fixtures it is (2026-08-07)
- WP-1.11: chose to seed a business-hours calendar and four priority-only SLA policies, which were previously API-created only, because "SLA breach examples exist" is unreachable in a clean-slate database without a policy for `SlaService.StartAsync` to match (2026-08-07)
- WP-1.11: chose to write `TicketSla` rows with elapsed business seconds and warning/breach flags already consistent with each other, over letting `SlaEvaluationJob` discover the breaches, because the job would otherwise re-escalate and reassign every seeded ticket and emit a notification per row on its first pass (2026-08-07)
- WP-1.11: chose a fixed random seed plus ids derived from (kind, ticket index, child index), with every random draw taken before the already-exists check, because a re-run must add nothing and a partially seeded database must continue producing the same dataset (2026-08-07)
- WP-1.11: chose transition history that follows the linear WP-1.2 chain (New→Triage→InProgress→Pending→Resolved→Closed) because that is the only path the seeded `ticket_status_transitions` table permits, so any other history would be one no agent could have produced (2026-08-07)
- WP-1.11: chose usernames (`enduser1`, `technician1`) as seeded requester and assignee ids, following the WP-1.7 seeded queue, over inventing identity subjects because Keycloak assigns a random `sub` per environment; the consequence is that seeded tickets fill the agent lists but not a real requester's portal (2026-08-07)
- WP-1.11: chose to seed 200 tickets over three months of ages weighted by status (New within days, Closed up to five months old) because a flat age spread makes every list look artificially uniform (2026-08-07)
- WP-2.1: chose table-per-hierarchy for CIs with typed per-type columns plus a separate definition/value pair of tables for user-defined fields, over a flat table of config-driven fields, because "type-specific fields enforced" must be real validation and the attributes must stay queryable and indexable (2026-08-07)
- WP-2.1: chose `CiTypeSchema` as the one description of each type's fixed attributes — driving write validation, read projection and the form's field list — because TPH makes every per-type column physically nullable, so enforcement has to live above the database (2026-08-07)
- WP-2.1: chose explicit column names for the properties two sibling types share (`server_hostname`/`virtual_hostname`, `server_ram_gb`/`virtual_ram_gb`, `network_vendor`/`software_vendor`) because EF's TPH uniquifier would otherwise emit a meaningless `hostname1` column (2026-08-07)
- WP-2.1: chose to reject attributes belonging to another CI type rather than ignore them because a client posting the wrong shape must fail loudly instead of silently dropping data (2026-08-07)
- WP-2.1: chose a new `CanManageAssets` policy (Admin/Technician/Manager) over reusing `CanManageTickets` because the latter deliberately includes EndUser for the portal, and the CMDB is an agent-only surface (2026-08-07)
- WP-2.1: chose filtered unique indexes on `asset_tag` and `serial_number` because they are the WP-2.5 import dedupe keys, and most CIs will have neither (2026-08-07)
- WP-2.1: chose to reject a custom field whose key collides with a built-in attribute of the same CI type (409) because two fields with one key would make the submitted-value dictionary ambiguous (2026-08-07)
- WP-2.1: chose to drop a CI custom-field value when the field is absent from an update payload, mirroring the create path, because clearing an optional field in the form must clear it in storage (2026-08-07)
- WP-2.1: chose to canonicalise `managementIp` through `IPAddress.Parse` and mirror the octet check in the browser because a loose regex lets "10.0.0.999" reach the API as a 400 the form cannot attribute to a field (2026-08-07)
- WP-2.2: chose to persist the lifecycle graph as an `assets.ci_lifecycle_transitions` table seeded by the migration, following the WP-1.2 ticket-status precedent, because the guard is then data an operator can read rather than a compiled-in switch (2026-08-07)
- WP-2.2: chose a lifecycle graph of the forward chain plus the real returns (Deployed→InStock, InRepair→Deployed/InStock, InStock→InRepair/Retired) and no shortcuts, so Ordered→Disposed is a 409 while a repaired laptop can go back out (2026-08-07)
- WP-2.2: chose to restrict a newly created CI to Ordered or InStock (400 otherwise) because every later state must be reached through a guarded transition or the history would have gaps no agent could have produced (2026-08-07)
- WP-2.2: chose to freeze a Disposed CI — no edits, no assignment, no outbound transitions (409 each) — and to clear IsActive on disposal, because a disposed record states what the asset was when it left the estate (2026-08-07)
- WP-2.2: chose to check a CI back in automatically when it is retired or disposed, writing the CheckIn row into the log, because a retired asset nobody holds must not keep a stale owner on the record (2026-08-07)
- WP-2.2: chose a new Platform `IDirectoryService` (users, departments, sites) with agent-only `/api/directory/*` endpoints over letting Assets read `platform.user_profiles`, because a module may never query another module's tables (2026-08-07)
- WP-2.2: chose to store owner/department/site ids on the CI with name snapshots beside them, following the WP-1.7 requester-snapshot precedent, because cross-schema joins are forbidden and the check-in/out log must stay readable after a person leaves the directory (2026-08-07)
- WP-2.2: chose "location" to mean an existing Platform site rather than a new Assets location entity because sites are already seeded and modelled, and a second location table would need reconciling with them (2026-08-07)
- WP-2.2: chose `PUT /api/cis/{id}/assignment` to be a complete statement of ownership — omitting the owner checks the CI in — because a partial-update assignment endpoint gives no way to express "nobody holds this any more" (2026-08-07)
- WP-2.2: chose to derive the assignment log's action (CheckOut/CheckIn/Transfer/Relocate) in the service from the before/after owner rather than have the client name it, because the client could then mislabel its own history (2026-08-07)
- WP-2.2: chose to serve the legal next states from `/api/ci-lifecycle-states` and render only those buttons, over duplicating the graph in the browser, because two copies of a guard drift (2026-08-07)
- WP-2.2: chose a right-side drawer per DESIGN.md §6 for lifecycle and ownership rather than a CI detail page because the asset 360° page is WP-2.4's scope (2026-08-07)
- WP-2.2: chose to prefill the department and site from the selected owner's profile in the drawer, both still editable, because assets usually follow their holder but a machine can sit at a site its owner does not work from (2026-08-07)
- WP-2.3: chose source = dependant and target = dependency for every relationship type ("this VM RunsOn that host"), so `ancestors` walks toward what a CI needs and `descendants` toward what needs it, because one direction convention has to hold or `impacted-by` means nothing (2026-08-07)
- WP-2.3: chose to accept cycles and traverse them safely — the CTE carries the path it took and refuses to re-enter a CI already on it, and the response carries a `containsCycle` flag — over rejecting a cycle-creating edge, because mutually dependent CIs (clustered pairs, redundant links) are real and a create-time reject would need a full graph walk on every insert (2026-08-07)
- WP-2.3: chose `impacted-by(id)` to be the `descendants` walk with the CI itself included at depth 0 because the blast radius of an outage includes the thing that failed, while `ancestors`/`descendants` answer the structural question and exclude their root (2026-08-07)
- WP-2.3: chose a keyless `CiGraphHop` entity read through `FromSqlInterpolated` over raw ADO because a recursive CTE has no LINQ equivalent and EF still parameterizes every value (2026-08-07)
- WP-2.3: chose to return every edge between the CIs a traversal reached, not only the edges it followed, because the edge that closes a cycle is exactly the one an operator needs to see and the walk by definition never traverses it (2026-08-07)
- WP-2.3: chose a hard depth ceiling of 10 with a default of 5, clamped rather than rejected and echoed back with a `maxDepthReached` flag, because a truncated answer must never look like a complete one (2026-08-07)
- WP-2.3: chose to block deleting a CI that still has relationships (409, with `Restrict` foreign keys behind it) over cascading the edges away, because an edge is a fact about two CIs and deleting one must not silently rewrite the other's graph (2026-08-07)
- WP-2.3: chose to refuse a self-relationship (400) and a duplicate (source, target, type) (409) while allowing the same pair related two different ways, because a VM can both run on and depend on its host but neither fact is worth storing twice (2026-08-07)
- WP-2.3: chose to refuse new relationships on a disposed CI (409), following the WP-2.2 freeze, because a disposed record states what the asset was when it left the estate (2026-08-07)
- WP-2.4: chose read ports in `Platform/Integration` implemented by the owning module (`ICiDirectory` → Assets, `ITicketLinkDirectory` → Helpdesk) over project references between modules, because Helpdesk and Assets each need to read the other and two project references would be a build cycle (2026-08-07)
- WP-2.4: chose to store only the CI id on a ticket link and read name, type, lifecycle state and owner live through the port, breaking with the WP-1.7/WP-2.2 snapshot precedent, because a CI is not a person who can leave the directory — the delete guard keeps it resolvable, so a rename must reach every ticket at once (2026-08-07)
- WP-2.4: chose to refuse deleting a CI that a ticket still links (409, checked through the port), because no foreign key can span the helpdesk and assets schemas and the ticket would otherwise point at a row that is gone (2026-08-07)
- WP-2.4: chose to enforce the ticket↔asset surface as agent-only inside the service with 403 ProblemDetails, following the WP-1.10 precedent, because `CanManageTickets` deliberately includes EndUser while the CMDB is an agent-only surface and auth configuration is outside this WP (2026-08-07)
- WP-2.4: chose `ciId` and `requester` filters on `/api/tickets` over dedicated endpoints on the assets side, because the 360° pages are exactly persisted filters and a list endpoint that already filters six ways should not be duplicated (2026-08-07)
- WP-2.4: chose to draw the relations mini-graph as depth-banded mini-cards in SVG-free markup rather than introducing React Flow, because DESIGN.md §9 reserves React Flow for the WP-4.3 topology maps and a card-sized graph carries no more than hop distance legibly (2026-08-07)
- WP-2.4: chose to render the mini-graph from the existing `ancestors` and `impacted-by` endpoints capped at 3 hops, echoing the server's `containsCycle` and `maxDepthReached` flags on the card, because a truncated picture must never look like a complete one (2026-08-07)
- WP-2.4: chose to key a person's assets by the directory user id and their tickets by username, because WP-1.11 seeded ticket identities as usernames while CI ownership stores directory ids; pinning both to one identity needs the realm-import WP (2026-08-07)
- WP-2.4: chose to add a People list and nav item alongside the user 360° page, which the WP text does not call for, because a page nothing links to cannot be verified by hand (2026-08-07)
- WP-2.5: chose ClosedXML 0.105.1 for `.xlsx` reading over shipping CSV-only or hand-rolling an OpenXML reader, because the WP text requires Excel; CSV is parsed in-repo (RFC 4180, ~80 lines) so the dependency is only reached for a workbook (2026-08-08)
- WP-2.5: chose one CI type per import file over a mappable type column, because the attribute list — and therefore the whole column-mapping form — is derived from the type (2026-08-08)
- WP-2.5: chose to refuse a mapping that maps neither asset tag nor serial number (400), because they are the only dedupe keys and without one a second run of the same file duplicates every row (2026-08-08)
- WP-2.5: chose serial-number-first then asset-tag matching because WP-2.1 put the filtered unique indexes on exactly those two columns for exactly this purpose (2026-08-08)
- WP-2.5: chose a mapped blank cell to mean "leave the stored value alone" rather than "clear it", because spreadsheet exports are full of blanks and an import must never wipe a field the operator did not mean to touch (2026-08-08)
- WP-2.5: chose to route every imported write through `ICiService.CreateAsync`/`UpdateAsync` rather than the DbContext, so an imported CI is validated, `CiTypeSchema`-bound, audited and published exactly like one typed into the form; the cost is one transaction per row (2026-08-08)
- WP-2.5: chose to re-send the file with each wizard step (columns → preview → commit) over parking the upload server-side, because nothing half-imported then has to be stored, expired or cleaned up (2026-08-08)
- WP-2.5: chose not to import lifecycle state at all, leaving every created CI at InStock, because every later state must be reached through the WP-2.2 guarded transition — a state change belongs to bulk edit (2026-08-08)
- WP-2.5: chose a repeated asset tag or serial inside one file to fail the later row, naming the earlier line, rather than merging the two, because a file that names one asset twice is a mistake the operator has to see (2026-08-08)
- WP-2.5: chose the dry run and the commit to return the same report shape from the same planning code, so the preview is literally what the commit will do (2026-08-08)
- WP-2.5: chose bulk edit to answer 200 with a per-CI report rather than failing the batch, because one disposed or illegally-transitioned CI must not stop the other 199 (2026-08-08)
- WP-2.5: chose bulk edit to delegate to `ICiLifecycleService.AssignAsync`/`TransitionAsync` rather than write columns, so the transition graph, the check-in/out log, the audit entry and the disposed-CI freeze all still apply (2026-08-08)
- WP-2.5: chose to apply the ownership change before the lifecycle move, because retiring a selection has to check its holders in and the automatic check-in runs on the final assignment (2026-08-08)
- WP-2.5: chose a CI already in the target state to count as a success rather than an illegal-transition failure, because selecting ten and deploying them should report ten successes when three were already deployed (2026-08-08)
- WP-2.5: chose to hold the bulk-edit selection as ids resolved against the current page, so changing a filter or turning a page silently drops what is no longer on screen rather than editing rows nobody can see (2026-08-08)
- WP-2.6: chose `DateOnly` (`date` columns) for contract start/end, purchase and warranty dates, breaking with the `DateTimeOffset` convention for timestamps, because an agreement ends on a calendar day and storing it as an instant makes the last day depend on the reader's timezone (2026-08-08)
- WP-2.6: chose a nullable `contract_id` on the CI over a join table, because an asset is covered by at most one support agreement and "the contract's CIs" is then a plain indexed query rather than a second table to keep consistent (2026-08-08)
- WP-2.6: chose to model `Vendor` as a first-class Assets entity while leaving the existing free-text `vendor`/`manufacturer` CI attributes untouched, because one names who made a thing and the other who the organisation has an agreement with; merging them is a data migration no WP has asked for (2026-08-08)
- WP-2.6: chose a separate `PUT /api/cis/{id}/coverage` over adding warranty fields to the CI create/update payload, following the WP-2.2 assignment endpoint, because the WP-2.5 importer builds `UpdateCiRequest` positionally and new optional members there would silently clear coverage on every imported update (2026-08-08)
- WP-2.6: chose coverage to be a complete statement — an omitted contract releases the CI — matching the assignment endpoint, because a partial-update payload gives no way to say "this is covered by nothing any more" (2026-08-08)
- WP-2.6: chose to read a CI's contract live through the relationship rather than snapshot its name, following WP-2.4's ticket-link rule, because a contract cannot leave the module and the delete guard keeps it resolvable (2026-08-08)
- WP-2.6: chose to notify at the tightest crossed threshold only (30, then 7, then the expiry day) rather than every crossed threshold, because a job that has not run for a month must say one true thing rather than fire three notices at once (2026-08-08)
- WP-2.6: chose the notification row itself as the dedupe key, with a unique index over (subject, subject id, due date, threshold), because that makes the pass idempotent and makes a renewal — a new end date — start a fresh 30/7/0 cycle instead of being silenced by the old one's notices (2026-08-08)
- WP-2.6: chose to commit the notification rows before sending the mail, because a failing SMTP host must not replay every notice on the next pass, and the WP asks for a notification that is *logged* (2026-08-08)
- WP-2.6: chose to compute contract and warranty status by comparing the end date to today rather than storing a status column, because a stored status is wrong every morning until something rewrites it (2026-08-08)
- WP-2.6: chose to expose `POST /api/contract-notifications/runs` alongside the daily Quartz trigger, because the dev database is recreated on most AppHost restarts, so a warranty created by hand to test the job would never survive to the next scheduled run; the pass is idempotent, so a manual trigger cannot raise anything twice (2026-08-08)
- WP-2.6: chose to list a contract's covered CIs through `/api/cis?contractId=` rather than a dedicated endpoint, following WP-2.4's "a 360° page is exactly a persisted filter" (2026-08-08)
- WP-2.6: chose to send warranty notices to the CI's holder (resolved through the Platform directory port) and contract notices to the contract's snapshotted owner email, both falling back to a configurable asset mailbox, because an unheld asset still needs someone to hear about it (2026-08-08)
- WP-2.7: chose the QR payload to be the asset's absolute URL (`{publicBaseUrl}/assets/{id}`) over an id or an encoded record, because a phone's own camera then opens the asset page with no app, no keyboard and no lookup step (2026-08-08)
- WP-2.7: chose a configured `Assets:Labels:PublicBaseUrl` falling back to `WebClient:Origin`, because a label carries its URL for the life of the sticker and "localhost" is only ever the host's own machine, never the phone's (2026-08-08)
- WP-2.7: chose QRCoder 1.8.0 (MIT) over hand-rolling QR encoding, because Reed–Solomon error correction and the mask-pattern rules are a specification to implement, not a helper to write (2026-08-08)
- WP-2.7: chose QuestPDF 2026.7.2 under its Community licence for PDF layout — the second file-format dependency after WP-2.5's ClosedXML — over emitting PDF by hand or printing HTML from the browser, because the WP asks for a batch PDF and label stock needs millimetre-accurate positioning (2026-08-08)
- WP-2.7: chose a single label to print on a page cut to the label itself and a batch to print as an A4 grid with cut guides, because the two are different printers — a dedicated label printer and a sheet of stock — rather than two sizes of the same document (2026-08-08)
- WP-2.7: chose to resolve a scanned code serial-number-first then asset-tag, case-insensitively, following WP-2.5's import dedupe order, because a scanner and an importer must never disagree about which CI a code names (2026-08-08)
- WP-2.7: chose an unknown id anywhere in a batch to fail the whole sheet with 404 rather than print the labels it could, because a short sheet is not noticed until the assets are already stickered (2026-08-08)
- WP-2.7: chose `/scan` to sit inside the authenticated agent shell rather than be an anonymous lookup, because an open endpoint would hand the CMDB to anyone who can reach the LAN and would break the EndUser scoping invariant (2026-08-08)
- WP-2.7: chose a typed/wedge-scanner input on `/scan` over an in-browser camera scanner, because `getUserMedia` needs a secure context and a LAN address served over plain HTTP is not one, so the camera button would be dead exactly where it is wanted (2026-08-08)
- WP-2.7: chose a `public-host` AppHost parameter (default `localhost`) with Keycloak, the API and the SPA pinned to 8080/5000/5173, over Aspire's dynamic port allocation, because a phone has to reach all three by an address it can resolve and dynamic ports change on every run (2026-08-08)
- WP-2.7: chose to render the realm import in AppHost — substituting `${PUBLIC_HOST}` into a copy under `obj/keycloak/` and bind-mounting that — over relying on Keycloak's own placeholder substitution, because probing Keycloak 26.3 directly showed **no** form resolves: `${PUBLIC_HOST}` stays literal (import fails on an invalid redirect URI), and both `${PUBLIC_HOST:localhost}` and `${env.PUBLIC_HOST:localhost}` silently collapse to their default whether the value is passed as a container environment variable or a `-D` system property. This supersedes the earlier claim on this line that the plain form substitutes, which was recorded as verified and was not (2026-08-08)
- WP-2.7: chose not to audit label printing, because generating a label reads the CMDB and writes nothing, and the audit invariant covers write endpoints (2026-08-08)
- WP-2.7: chose to bind the pinned endpoints to `0.0.0.0` through Aspire's `EndpointAnnotation.TargetHost`, gated on `public-host` being something other than `localhost`, over binding unconditionally or leaving Aspire's default, because pinning a port number does not make it reachable — DCP proxies to loopback, so the LAN address was refused until this changed — and an unconditional bind would expose every dev run, including the Keycloak admin console, to the network (2026-08-08)
- WP-2.7: chose to read `public-host` from `builder.Configuration["Parameters:public-host"]` rather than rely on `AddParameter(name, value)`'s default, because that overload takes a *given* value and the host is also needed as a plain string at build time to decide the bind address (2026-08-08)
- WP-2.7: chose to accept that the phone leg of the scan cannot be completed over plain HTTP and to defer it to WP-6.2 (Proxy + TLS) rather than add HTTPS here, because oidc-client-ts always does PKCE for `response_type: 'code'` and `crypto.subtle` exists only in a secure context — the same rule that already ruled out an in-browser camera scanner — so the sign-in button throws before it navigates and no amount of host, port or realm configuration changes it (2026-08-08)
- WP-2.8: chose to write the seeded estate through `AssetsDbContext` rather than `ICiService`, because the service is one transaction, one audit entry and one outbox message per CI — right for an operator's edit, wrong for 60 rows nobody performed; the cost is that `CiTypeSchema` no longer applies itself, so the seeder binds every CI through it explicitly and a unit test asserts the whole estate conforms without a database (2026-08-08)
- WP-2.8: chose a hand-written estate table over generated data with a fixed random seed, breaking with WP-1.11's approach, because 60 named devices have to read like a real organisation's while 200 tickets only have to read like traffic; ids derive from array position, which makes appending safe and reordering a renumbering (2026-08-08)
- WP-2.8: chose three site-local dependency trees (Primary Data Centre, Head Office, Regional Branch) with no WAN edges between them, because the WP asks for two to three trees and joining the sites would make every router's `impacted-by` the whole estate rather than a bounded answer somebody can read (2026-08-08)
- WP-2.8: chose to write lifecycle history rows directly instead of walking `ICiLifecycleService`, following WP-1.11's ticket history precedent, while restricting every written pair to an edge the seeded transition graph permits and asserting that in an integration test — so the seeded history is one an agent could actually have produced (2026-08-08)
- WP-2.8: chose to seed ticket↔CI links from a second seeder living in Helpdesk that takes the CI ids as an argument, over letting the Assets seeder write them, because Helpdesk owns the link and neither module may reference the other (2026-08-08)
- WP-2.8: chose to validate the platform directory up front and fail with one sentence, over resolving each owner as its row is written, because a run against a database without the platform demo data must stop before writing half an estate (2026-08-08)
- WP-2.8: chose both seeded CI custom fields to be optional, breaking with WP-1.9's deliberately-required ticket field, because a required CI custom field would make every CI created by hand — and every existing integration test that posts one — fail validation (2026-08-08)
- WP-2.8: chose contract, purchase and warranty dates as offsets from the day the seeder runs rather than fixed calendar dates, because the dev database is recreated on most AppHost restarts and fixed dates would silently drift into the past (2026-08-08)
- WP-2.8: chose to seed agreements and warranties that sit inside the 30-day and 7-day windows and past their end date, so the WP-2.6 renewal job has something true to find on its first pass instead of a database where the feature cannot be seen (2026-08-08)
- WP-2.9: chose the CI whose page is open to always be the new edge's *source* — the dialog writes one sentence, "this CI ⟨verb⟩ that CI" — over a direction toggle, because WP-2.3 fixed source → target as "source needs target" and the server's 400 names `TargetCiId`, so one target field is the only thing an inline field error can attach to; the reverse edge is created from the other CI's page, where that sentence is the one being written (2026-08-08)
- WP-2.9: chose to offer every candidate in the picker — the CI itself, a disposed CI, an already-related pair — and render the server's refusal beside the chosen CI, over filtering them out client-side, because a guard the browser hides is a guard nobody can watch working, and a second copy of the rule in the SPA would drift from the one in `CiRelationshipService` (2026-08-08)
- WP-2.9: chose to make only a CI's *direct* edges editable, listed above the existing hop-banded graph rather than drawn into it, because the bands carry nodes by hop distance and an edge two hops out belongs to some other CI's page (2026-08-08)
- WP-2.9: chose WP-2.4's two-step in-row confirm ("Remove" → "Confirm remove") for deleting an edge over the `window.confirm` used for deleting a contract or vendor, because removing one relationship is reversible in a click and the row is a clearer place to ask than a browser dialog that names no row (2026-08-08)
- WP-2.10: chose to represent "mixed" as a null `CiType?` on the mapping (the string `Mixed` on the wire) over adding a `Mixed` member to `CiType`, because `CiType` is the TPH discriminator stored on every CI row and a value no CI can ever have does not belong in it (2026-08-08)
- WP-2.10: chose the mixed mapping form to offer the union of every type's attributes with required-ness carried per type, over one form per type, because in a mixed file the type is a property of the row and is not known until the row is read (2026-08-08)
- WP-2.10: chose a mapped type column to be authoritative — never second-guessed by inference, and a blank cell in it refused rather than inferred — because a column the operator mapped is their statement of where the type comes from, and silently falling back would make one file use two rules (2026-08-08)
- WP-2.10: chose to derive the inference discriminators from `CiTypeSchema` (the attribute keys exactly one type declares) rather than listing them by hand, so an attribute later added to a second type stops being a discriminator by itself instead of quietly guessing wrong (2026-08-08)
- WP-2.10: chose a row whose type is ambiguous or undeterminable to be refused by line number, over resolving it by a priority order, because the dry run is the safety net and a rule nobody can see is exactly what it exists to prevent (2026-08-08)
- WP-2.10: chose to enforce "never commit an inferred type the operator has not seen" server-side with an `acceptInferredTypes` flag the wizard only sets after the dry run, over trusting the wizard's step order, because a TPH type is permanent — the wrong guess is undone only by deleting the CI, which the WP-2.3/2.4 guards block the moment anything names it (2026-08-08)
- WP-2.10: chose a mixed row to ignore mapped columns belonging to other types while a single-type import keeps rejecting them, because in a mixed file another type's column is that row's blank, but in a single-type file the operator declared the whole file to be one shape (2026-08-08)
- WP-2.11: chose to draw both dependency directions as indented trees built by walking the traversal's own edges, over the hop-banded rows they replace, because the endpoints stamp each CI with a distance but never with the route, and "reached *through* the switch" is the thing an operator reads a dependency card for (2026-08-09)
- WP-2.11: chose to draw a CI reachable by two routes once, marked, rather than expanding it under every parent, because a diamond would otherwise reprint a whole subtree per parent and a cycle would not terminate (2026-08-09)
- WP-2.11: chose to read the direct-relationship lifecycle pills out of the traversal nodes, because every directly related CI is a depth-1 node of one walk and `CiRelationship` carries no lifecycle — asking for one would have made a frontend-only package a backend change (2026-08-09)
- WP-2.11: chose per-direction depth with a "Show deeper" button up to the server's 10-hop ceiling, over the hardcoded 3 hops and its dead-end truncation notice, because a truncated answer the reader cannot extend is indistinguishable from a complete one (2026-08-09)
- WP-2.11: chose sentence-case section headings against the mock's ALL-CAPS, because DESIGN.md §4 forbids caps headers everywhere and the mock is a sketch, not the design system (2026-08-09)
- WP-2.11: chose the header count to be the CI's *direct* edges rather than every edge the traversal reached, because the old number counted a 3-hop walk and printed it above a list of four rows (2026-08-09)
- WP-2.11: chose a `formatDateOnly` helper that reads the date parts rather than parsing the string, because `new Date('2026-09-14')` is UTC midnight and renders as the 13th west of Greenwich — WP-2.6 stores these as `DateOnly` precisely because an agreement ends on a day (2026-08-09)
- WP-2.11: chose to distinguish an empty history from an unreadable one on the CI page's two log cards, because answering a failed request with "Nobody has held this asset yet" states a fact about the asset that nothing established (2026-08-09)
- WP-2.11: chose KPI tiles that apply the filter they count and clear it when pressed again, over static figures, because a number nobody can interrogate is decoration; the counts come from the list endpoint under each tile's own filter so the tile and the table can never disagree (2026-08-09)
- WP-2.11: chose to omit DESIGN.md §6's "from last week" delta from the KPI tiles, because nothing in the CMDB records a historical count and a computed delta would be decoration presented as measurement (2026-08-09)
- WP-2.11: chose a failed count to read "Unavailable" rather than 0, following the same rule as the history cards: a zero is a claim about the estate (2026-08-09)
- WP-2.11: chose lifecycle to sort by its place in the WP-2.2 state graph rather than alphabetically, because "Deployed, Disposed, In repair, In stock" orders spellings and nobody reads a lifecycle column looking for the letter D (2026-08-09)
- WP-2.11: chose blank cells to sort last in both directions, because a CI with no serial is missing a value rather than holding the smallest one, and a descending sort would bury the rows the operator asked to see (2026-08-09)
- WP-2.11: chose to state "sorted within this page" in the footer whenever a sort is active on a multi-page estate, over sorting silently, because `/api/cis` orders by name and takes no sort parameter, so a browser sort after paging is a partial answer that otherwise looks total (2026-08-09)
- WP-3.1: chose a monitored device that stores only its polling address and reads the CI's name, type and site live through `ICiDirectory`, over snapshotting them, following WP-2.4's ticket-link rule — the address you poll is a monitoring fact, everything else belongs to the CMDB (2026-08-10)
- WP-3.1: chose one monitored device per CI (unique index on `ci_id`) and an immutable CI on the device, because two devices for one CI would hold two opinions about whether it is up, and repointing a device would silently re-attribute every metric and alert already recorded against it (2026-08-10)
- WP-3.1: chose an append-only `monitoring.config_changes` table carrying an explicitly allocated version, over computing a diff between two rendered documents, because a diff cannot express a deletion and cannot see a device created and deleted between two fetches (2026-08-10)
- WP-3.1: chose to allocate config versions under `pg_advisory_xact_lock` rather than an identity column, because identity values can commit out of order — a poller reading `max(version)` while a lower version was still uncommitted would never see that change again (2026-08-10)
- WP-3.1: chose the delta's unit to be the whole device — a check edit re-sends its device — because the poller's unit of work is a device, and a partial device would have to be merged correctly by every poller implementation (2026-08-10)
- WP-3.1: chose the delta to decide "changed" against "removed" by whether the device is *currently* enabled and in the poller's group, rather than by the change kind recorded at the time, because one rule then resolves deletion, disabling and a move between groups identically (2026-08-10)
- WP-3.1: chose a device that moves poller groups to write two changes — an upsert against the new group and a removal against the old — because otherwise the group it left keeps polling a device that is no longer its responsibility (2026-08-10)
- WP-3.1: chose to send maintenance windows in full on every config fetch rather than delta them, because there are few of them and a poller that mutes the wrong device is worse than a poller that re-reads a short list (2026-08-10)
- WP-3.1: chose to refuse a `sinceVersion` ahead of the server with 400, over quietly falling back to a full snapshot, because a poller holding a version this server never issued is reading a restored or foreign database and a delta against the wrong past is worse than an error (2026-08-10)
- WP-3.1: chose a new additive `CanManageMonitoring` policy (Admin/Technician/Manager) over reusing `CanManageAssets`, following the WP-2.1 precedent, so the monitoring surface can diverge later without editing a policy two modules already depend on (2026-08-10)
- WP-3.1: chose to leave the poller registration and config-fetch endpoints behind the operator policy as an interim, because the poller has no identity of its own until WP-3.2 issues it credentials; a poller must not need an agent's rights to read its own configuration (2026-08-10)
- WP-3.1: chose the config fetch to record the version the poller now holds — the only write behind a GET in this module — and not to audit it, because a poller reading its configuration has no before/after entity state and WP-3.2's heartbeat needs to know which pollers are behind (2026-08-10)
- WP-3.1: chose to publish no events from device, check or maintenance-window writes, following the WP-2.6 contracts/vendors precedent, because nothing consumes them yet and an event nobody handles is a guess about WP-3.5's shape (2026-08-10)
- WP-3.1: chose checks to cascade away with their device while a CI relationship still blocks a CI delete, because a check is a property of one device rather than a fact about two peers (2026-08-10)
- WP-3.1: chose a maintenance window's scope to be a complete statement, and an estate-wide window that also names devices to be refused rather than have its list ignored, because silently dropping the list leaves the operator believing they had scoped it (2026-08-10)
- WP-3.1: chose `CheckRules` as a pure cross-field validator beside the FluentValidation rule set, because interval-versus-timeout and warning-versus-critical are relationships between fields that a per-property rule cannot see, and the whole matrix is then unit-testable without infrastructure (2026-08-10)
- WP-3.2: chose a Keycloak service account (`it-platform-poller`, client credentials, `Poller` realm role) over a shared API key, because the platform already has exactly one authentication mechanism and a second one would need its own issuing, rotation and revocation story (2026-08-10)
- WP-3.2: chose `CanPoll` to be disjoint from every operator policy — the `Poller` role only, not even Admin — and the fleet list to stay on `CanManageMonitoring`, because "a poller must not need an agent's rights" is only true if the reverse holds too (2026-08-10)
- WP-3.2: chose to pin Keycloak's issuer with `KC_HOSTNAME` derived from `public-host`, over accepting a second valid issuer in the API, because the poller is the first client inside a container and must dial `host.docker.internal` while presenting a token the API's single configured authority accepts (2026-08-10)
- WP-3.2: chose a rendered RabbitMQ definitions file bind-mounted into the broker, following the WP-2.7 realm-rendering precedent, over creating the poller's account with a start-up script, because the account, its permissions and the exchange it cannot declare are one document that either imports or fails loudly (2026-08-10)
- WP-3.2: chose to put the definitions renderer in `Platform/Messaging` rather than in AppHost, so the integration tests import the exact document that ships; a permission model asserted against a hand-written copy proves nothing about the one that runs (2026-08-10)
- WP-3.2: chose the poller's bus rights to be an empty `configure` pattern, an empty `read` pattern and a `write` pattern anchored to the one heartbeat exchange, because RabbitMQ has no publish-only flag and an empty pattern is how "no rights at all" is spelled (2026-08-10)
- WP-3.2: chose to declare the heartbeat exchange in the definitions file with MassTransit's own name, type and durability, because the poller has no permission to declare it and a mismatched declaration would fail the API's bus start-up with PRECONDITION_FAILED (2026-08-10)
- WP-3.2: chose the heartbeat to carry its own `IntervalSeconds` rather than the server storing a configured interval per poller, because the poller is the only thing that knows its cycle and "missed N heartbeats" is meaningless without that number (2026-08-10)
- WP-3.2: chose the heartbeat consumer to be idempotent by construction — the stored heartbeat only moves forward — rather than to use the Platform dedupe helper, because a dedupe row per beat is a row every fifteen seconds per poller forever, to protect an update that is already safe to repeat; this is a deliberate departure from ARCHITECTURE.md §4's "use the dedupe helper" and is the only one in this package (2026-08-10)
- WP-3.2: chose `PollerHeartbeatMissed` to be published once per silence — marked on the poller row, cleared by the next beat — over once per evaluation pass, because a poller that stays down would otherwise emit a message a minute for as long as it is off (2026-08-10)
- WP-3.2: chose to mark the row before publishing, over publishing first, because a failed publish then leaves a record that the silence was seen rather than a message about an outage nothing remembers (2026-08-10)
- WP-3.2: chose a poller that has never sent a heartbeat to be skipped by the evaluator, because it has no interval to be late by and a registration that never started is a deployment problem rather than an outage (2026-08-10)
- WP-3.2: chose to audit the missed heartbeat under a system actor, which is also what flushes the outbox, following the WP-3.1 rule that a poller reading config is not audited but a fact about an outage is worth dating (2026-08-10)
- WP-3.2: chose to raise an event rather than an alert or a ticket, because the alert state machine is WP-3.5 and alert→ticket is WP-3.6; both consume this instead of re-deriving it (2026-08-10)
- WP-3.2: chose to run the poller as an `AddDockerfile` container rather than an Aspire Python executable resource, because "`docker stop` the poller and watch the platform notice" is a verification step and an executable resource cannot be stopped that way (2026-08-10)
- WP-3.2: chose module consumers to be handed to Platform's single `AddMassTransit` through an optional `configureBus` callback, over Platform naming them or each module calling `AddMassTransit` itself, because MassTransit is configured once per host while a consumer belongs to the module that reacts — and Platform may not reference a module (2026-08-10)
- WP-3.2: chose the poller's cycle to swallow every failure — registration, config fetch, publish — and keep its previous configuration, because the rule that one dead device must never abort a cycle applies to the poller's own dependencies, and a poller that exits on a restarting API is one somebody has to go and start again (2026-08-10)
- WP-3.2: chose a poller whose `sinceVersion` the server refuses to forget everything and take a full snapshot in the same cycle, over waiting for the next one, because it is otherwise blind for an interval while holding a configuration the server has disowned (2026-08-10)
- WP-3.2: chose to omit `sourceAddress` and `destinationAddress` from the poller's MassTransit envelope entirely, over building valid URIs for them, because MassTransit parses both as absolute URIs before any consumer runs and the exchange name contains a colon — they are optional, carry nothing this consumer needs, and every field kept is a field that has to stay parseable (2026-08-10)
- WP-3.2: chose a committed JSON fixture as the contract between the Python publisher and the .NET consumer — asserted by `test_bus.py` on one side and by `PollerEnvelopeTests` on the other, the latter reading it with MassTransit's own serializer options — because each side testing its own idea of the envelope is exactly how a dead-lettering bug passed a green suite (2026-08-10)
- WP-3.2: chose to assert the envelope contract in process rather than by publishing the fixture's raw bytes through the broker, because the round trip made the assertion depend on exchange bindings and consumer start-up timing while the bug it guards against is a deserialisation failure; the transport is proved separately by the MassTransit-published test and by the live run (2026-08-10)
- WP-3.2: chose to give the publish-only credential test a RabbitMQ container of its own, because importing a definitions document is broker-wide and doing it against the shared fixture broker silently unseated the MassTransit endpoint another test in the same collection depends on (2026-08-10)
- WP-3.2: chose to truncate both sides of the heartbeat's monotonic comparison to microseconds, because a `DateTimeOffset` keeps 100ns ticks and a `timestamptz` does not, so a redelivered message read as newer than itself — the same precision trap WP-3.1 hit in a test, here in production code (2026-08-10)
- WP-3.3: chose two new events — a batched `DeviceTelemetryReported` per cycle and an edge-triggered `DeviceReachabilityChanged` — over one message per check, because a poller with 200 devices and 4 checks each would otherwise publish 800 messages a cycle to say the same thing, and a device that is down for an hour would repeat it 240 times (2026-08-10)
- WP-3.3: chose to widen the poller's RabbitMQ write permission to an anchored alternation of three literal exchange names (`RabbitMqDefinitions.PollerExchanges`) rather than a `Contracts\.Events:.*` prefix, because a prefix is a licence to forge a `TicketCreated`; `configure` and `read` stay empty and a test asserts the pattern matches no other event's exchange (2026-08-10)
- WP-3.3: chose `icmplib` (MIT) for ICMP after asking, over hand-rolling the protocol or parsing `ping` output, because checksums, sequence matching and per-locale output parsing are all worse than a dependency; `pysnmp` is named by the WP text (2026-08-10)
- WP-3.3: chose an ICMP **datagram** socket with a `net.ipv4.ping_group_range` sysctl over `--cap-add=NET_RAW`, because the capability does not work for a non-root container at all — Docker puts it in the permitted set and a process with no file capability on its binary has an empty effective set, so the raw socket still fails with "Root privileges are required". Probed both ways in the built image. Granting one uid the right to ping is also narrower than granting the container raw-socket access (2026-08-10)
- WP-3.3: chose per-check due times against a **monotonic** clock inside the existing cycle, over a task per check or a cycle that runs everything, because a five-minute check on a fifteen-second cycle should cost twenty cycles of nothing, and an NTP correction must not make every check in the estate overdue at once (2026-08-10)
- WP-3.3: chose to mark a check run when it is handed out rather than when it completes, because the interval is a period and a check that hangs to its timeout must not be started again by the next cycle (2026-08-10)
- WP-3.3: chose an ICMP check to decide a device's reachability on its own where there is one, falling back to "any check that completed" where there is not, because an SNMP timeout on a device that still pings is a credential or agent problem rather than an outage (2026-08-10)
- WP-3.3: chose the first observation of a device to be published as a reachability change, because the platform holds no state for a device it has never polled; the cost is one event per device on a poller restart and the alternative is a platform that knows nothing until something changes (2026-08-10)
- WP-3.3: chose the recovery event to carry the length of the outage that just ended rather than zero, because the cycles in between publish nothing and it is the only place a consumer can read that number (2026-08-10)
- WP-3.3: chose a failed check to travel in the telemetry batch with its reason, over omitting it, because a timeout is a fact about the device and an omitted check reads as one nobody asked about (2026-08-10)
- WP-3.3: chose a metric to carry either a number or text and never both, because a hypertable stores numbers and a device record stores names, and WP-3.4 has to tell them apart without a lookup table of metric names (2026-08-10)
- WP-3.3: chose an SNMP metric family per check (`sysinfo`/`cpu`/`memory`/`oid`) over one check returning everything, so a device's CPU and its inventory can carry their own intervals and thresholds instead of both running at whichever is busiest (2026-08-10)
- WP-3.3: chose `cpu` and `memory` to read host-resources first and fall back to UCD-SNMP, and a device answering neither to report a **failed** check rather than zero, because zero percent CPU and "does not report CPU" are different facts and one of them is alarming (2026-08-10)
- WP-3.3: chose to put every pysnmp-shaped type behind an `SnmpTransport` protocol, so which OIDs a metric reads, how a processor table averages and what a missing row means are all testable without a socket or a device (2026-08-10)
- WP-3.3: chose to publish nothing at all for a cycle with no results, over an empty batch, because an empty message every fifteen seconds per poller is noise nothing consumes (2026-08-10)
- WP-3.3: chose telemetry that fails to publish to be dropped rather than queued, because the next cycle measures again and a poller that replayed an hour of readings after a broker outage would stamp them all as if they had just happened (2026-08-10)
- WP-3.3: chose to pull snmpsim forward from WP-3.12 after asking, serving several device profiles from one container by community string, because "metrics flow" and "stop the target → down event" are otherwise unverifiable without hardware (2026-08-10)
- WP-3.3: chose a seeded device at 192.0.2.1 (RFC 5737, routed nowhere) over unplugging something, because "one dead device never blocks the cycle" should be watchable on a fresh `aspire run` rather than taken on trust (2026-08-10)
- WP-3.3: chose to give the simulator **no published endpoint** and address it as `snmpsim:161` on the Aspire session network, over publishing a host port, because a published endpoint goes through DCP's proxy which binds loopback — it answers from the host and not from the poller's container, which is where every SNMP check comes from; the first live walk of this package found exactly that (2026-08-10)
- WP-3.3: chose the monitoring seeder to write through the DbContext **and** `IMonitoringConfigLog` in one transaction, saving per device, because a device written without a config-change row is invisible to every poller forever — and because `RecordAsync` allocates its version from `max(version)` in the database, so two unsaved calls in one transaction collide on the primary key (2026-08-10)
- WP-3.3: chose the monitoring seeder to take CI ids as an argument, following WP-2.8's ticket↔CI links, because Monitoring may not reference Assets and a device naming a CI that does not exist reads as a null name on every screen (2026-08-10)
- WP-3.3: chose not to act on maintenance windows in the poller, although WP-3.1 sends them, because muting belongs to the alert state machine (WP-3.5) and a poller that stopped measuring during a window would leave a hole in the metrics exactly where a change was being made (2026-08-10)
- WP-3.4: chose one hypertable keyed naturally on (time, device, check, metric name) over a surrogate id, because Timescale requires every unique index to carry the partitioning column and a natural key makes a redelivered batch's `ON CONFLICT DO NOTHING` a no-op (2026-08-10)
- WP-3.4: chose a one-day chunk interval over Timescale's seven-day default, because retention drops whole chunks and the chunk width is therefore the granularity at which the thirty-day policy actually removes anything (2026-08-10)
- WP-3.4: chose to install retention and the five-minute rollup as Timescale background jobs written into the EF migration as raw SQL, over deleting rows or aggregating in application code, because a scheduler the database owns keeps running when the host is down and nothing in C# then has a reason to delete a metric (2026-08-10)
- WP-3.4: chose real-time aggregation on the continuous aggregate (`materialized_only = false`), against Timescale's 2.13+ default, because the refresh policy leaves the newest five to ten minutes unmaterialised and that is exactly the part of a monitoring chart somebody is watching (2026-08-10)
- WP-3.4: chose `text` columns on the hypertable, against the `varchar(n)` the rest of the schema uses, because `create_hypertable` warns about every length-limited column it is handed; the lengths are enforced by the ingestion planner instead (2026-08-10)
- WP-3.4: chose to store text samples (`sysDescr`, `sysName`) in a `monitoring.device_inventory_facts` table of latest-value-per-device rather than in the hypertable, because WP-3.3 split `MetricSample.Value` from `.Text` precisely so this package could tell a series from a current-state fact (2026-08-10)
- WP-3.4: chose to derive `check.success` (0/1) and `check.latency_ms` from every check result and reserve the `check.` prefix against poller-supplied names, because a failed check carries no samples and would otherwise be indistinguishable from a device nobody polls — and averaged over a bucket the success series is availability (2026-08-10)
- WP-3.4: chose the Platform dedupe helper for the telemetry consumer, unlike WP-3.2's heartbeat consumer, because this writes N rows across two DbContexts and can fail half way where a forward-only column update cannot; the hypertable's natural key covers the window between the two transactions (2026-08-10)
- WP-3.4: chose a parameterised multi-row `INSERT ... ON CONFLICT DO NOTHING` over EF's change tracker for ingestion, because a cycle is an append-only bulk insert with a conflict clause EF cannot express; every value is still a parameter (2026-08-10)
- WP-3.4: chose to refuse a raw-resolution series longer than 24 hours (400) rather than truncate or silently downsample it, following WP-2.3's traversal-depth rule that a partial answer must never look like a complete one; `Auto` switches to the rollup at six hours (2026-08-10)
- WP-3.4: chose to consume only `DeviceTelemetryReported` and leave `DeviceReachabilityChanged` to WP-3.5's state machine, storing each check's outcome as the `check.success` series instead, because up/down history is then queryable without this package guessing the alert engine's shape (2026-08-10)
- WP-3.4: chose the metric picker to be discovered from the data (`DISTINCT ON` over the last two days) rather than declared anywhere, because which metrics a check produces is the poller's business and a hand-kept list would drift (2026-08-10)
- WP-3.4: chose a series over a metric name that several checks report to be refused (400, naming the candidate check ids) unless the caller passes `checkId`, over interleaving them or picking one, because every check contributes `check.success` and `check.latency_ms` — on the seeded four-check router that is four series per name, and plotted together an ICMP reading of 0.03 ms sits beside an SNMP one of 522 ms (2026-08-10, found by hand-verification)
- WP-3.4: chose the metric picker to list one entry per metric *and* check, carrying the check's name, over one per metric, because a picker that collapsed them would hide three of the four series a device actually has (2026-08-10)
- WP-3.5: chose every alert rule to be per *check* and driven from `DeviceTelemetryReported`, and deliberately did **not** consume `DeviceReachabilityChanged`, because that event is edge-triggered — one message per transition — and a "for N cycles" condition cannot be evaluated against a signal that does not arrive every cycle; the ICMP check's availability rule answers the same question from telemetry that does (2026-08-10)
- WP-3.5: chose two rule families per check — an **availability** rule from whether the check completed, and a **threshold** rule on the one metric the check is about — over a rule per reported metric, because an SNMP CPU read carries a figure per core beside the average and alerting on each would turn one busy host into nine alerts (2026-08-10)
- WP-3.5: chose `check:{checkId}:availability` and `check:{checkId}:{metric}` as rule ids, derived from the check id rather than allocated, because WP-3.6 dedupes tickets on `alert:{deviceId}:{ruleId}` and that only works if the id is identical across a restart and on every recurrence (2026-08-10)
- WP-3.5: chose a durable `monitoring.alerts` row beside the Redis state, with the state rebuilt from the open row on a miss, because ARCHITECTURE §5 forbids Redis holding anything that must survive a flush — without it a flush would re-raise every open alert and WP-3.6 would open a second ticket for each (2026-08-10)
- WP-3.5: chose a filtered unique index on `(device_id, rule_id) WHERE status = 'Open'`, so "raised exactly once" is a database constraint rather than only a state-machine invariant and two consumers racing on one rule cannot both open an alert (2026-08-10)
- WP-3.5: chose publication to be driven by a difference between the rule's actual severity and the last severity anyone was told, rather than by the transition that produced it, because that one rule makes "raised once", "cleared once" and "reconcile after suppression" the same mechanism instead of three (2026-08-10)
- WP-3.5: chose suppression (maintenance window, flapping) to withhold the *publication* while the rule keeps evaluating and the alert row keeps updating, over pausing evaluation, because a window that ends over a device that is still down has to speak, and a change nobody can see afterwards makes the window itself unauditable (2026-08-10)
- WP-3.5: chose the state change that trips the flap threshold to be itself withheld, because the message proving a rule is flapping is exactly the kind the policy exists to stop (2026-08-10)
- WP-3.5: chose flap flips to be counted on the rule's own state changes rather than on published messages, because counting published ones would let suppression hide the evidence that suppression is still needed (2026-08-10)
- WP-3.5: chose hysteresis as a percentage of the threshold applied only when the rule is already at that level, and the *configured* threshold to be what an alert reports, because an alert that says "above 66.5" when the form says 70 is one nobody can reconcile (2026-08-10)
- WP-3.5: chose recovery to be quicker to believe than failure (2 cycles against 3) because an alert that outlives its problem is what teaches people to ignore alerts (2026-08-10)
- WP-3.5: chose a failed check to leave its threshold rule un-advanced rather than to read as a good value, because nobody measured the CPU of a switch that did not answer and the alternative clears a threshold alert at the moment the device disappears (2026-08-10)
- WP-3.5: chose five nullable per-check tuning columns over a platform-wide constant or a JSON blob, with null meaning "the platform default", so a check never stores a copy of the defaults to drift from them (2026-08-10)
- WP-3.5: chose a second consumer on `DeviceTelemetryReported` beside WP-3.4's ingestion rather than evaluating inside it, because storing a reading and judging it have different failure modes and MassTransit gives each consumer its own queue — a faulting alert engine must not stop metrics being written (2026-08-10)
- WP-3.5: chose the Platform dedupe helper for the alert consumer, unlike WP-3.2's heartbeat, because the state machine counts consecutive readings and a redelivered batch would advance every "for N cycles" counter a second time (2026-08-10)
- WP-3.5: chose to audit each raise and clear under the system actor, following WP-3.2's missed heartbeat, which is also what flushes the outbox (2026-08-10)
- WP-3.5: chose to build the engine only, with no `/api/alerts` read surface, because WP-3.9 owns the alert board and an endpoint written a package early is a guess about what it needs (2026-08-10)
- WP-3.6: chose the alert→ticket consumers to live in `Modules.Helpdesk`, not in Monitoring, because a consumer belongs to the module that reacts and Helpdesk owns tickets; Monitoring is never asked about one and neither module reads the other's tables (2026-08-11)
- WP-3.6: chose a durable `helpdesk.alert_tickets` row with a unique index on the dedupe key over holding the mapping in Redis, because "one ticket per alert" has to survive a flush and a restart (ARCHITECTURE §5) and a unique index makes two racing consumers a failed transaction rather than two tickets — the same argument WP-3.5 made for its filtered unique index (2026-08-11)
- WP-3.6: chose the rate limit and the circuit breaker to live in Redis, which is where ARCHITECTURE §5 puts rate-limit state, but the breaker to fall back to counting `alert_tickets` rows in the window when Redis is unreadable, because a bound that disappears with the cache is not a bound and a storm during a cache outage is when a ticket flood does most damage (2026-08-11)
- WP-3.6: chose the rate limit to be allowed through on a Redis failure while the breaker is not, because a per-rule ticket is already capped at one by the durable dedupe row — the worst case is a second ticket for a rule that flapped — whereas the breaker is the only thing standing between fifty distinct alerts and fifty tickets (2026-08-11)
- WP-3.6: chose the circuit breaker to be global across every rule while the rate limit is per rule, because fifty distinct alerts each pass their own per-rule limit and that is exactly the storm the WP asks the breaker to stop (2026-08-11)
- WP-3.6: chose the breaker to drop its counting window when it trips, because a window left in place is still over the threshold when the cooldown expires and would re-trip on the next alert — a breaker that never closes (2026-08-11)
- WP-3.6: chose the admin notice to fire once per trip, claimed with a Redis SETNX so whichever consumer crosses the line owns it, over one notice per suppressed alert, because a breaker that mails fifty times is the flood it exists to prevent (2026-08-11)
- WP-3.6: chose a raise the automation refuses to still write its `alert_tickets` row with a suppression count, over dropping it, because a storm has to be legible afterwards and an alert that opened no ticket is the thing an operator will be looking for (2026-08-11)
- WP-3.6: chose auto-resolve to walk the WP-1.2 transition graph one guarded hop at a time — four transitions from New — over adding a shortcut edge to Resolved, because the seeded graph is the workflow every human agent follows and changing it for an automation is a different package's decision; each hop goes through `ITicketService.TransitionAsync` so the guard, the history row, the SLA clock and the audit entry all still happen (2026-08-11)
- WP-3.6: chose the route to be found breadth-first over the `ticket_status_transitions` rows rather than hardcoded, so a shortcut somebody adds later is used and a missing edge fails to find a path instead of walking into a 409 per hop (2026-08-11)
- WP-3.6: chose a rule that recurs after its ticket was resolved or closed to open a successor ticket, with a note on each pointing at the other, because the WP-1.2 graph has no edge back out of Resolved so "reopen" is not available; a silent second ticket would be indistinguishable from the duplicate this package exists to prevent (2026-08-11)
- WP-3.6: chose recurrences and clears to be recorded as **internal** comments, because the requester of an automated ticket is the platform itself — a public comment would mail nobody while looking like it had mailed somebody, and WP-1.7 already rules that only agent-authored public comments are emailed (2026-08-11)
- WP-3.6: chose the automation never to close a ticket, only to resolve it, because confirming a resolution belongs to a requester or an agent (WP-1.8) and is not something an automation gets to assume (2026-08-11)
- WP-3.6: chose severity to set urgency and impact rather than the priority directly, so an automated ticket lands where `TicketPriorityMatrix` says it should instead of being the one ticket in the system that disagrees with the matrix (2026-08-11)
- WP-3.6: chose the Platform dedupe helper for both consumers, because the durable row stops a second *ticket* but a redelivered message would still add a second annotation and count a second occurrence — quiet double-entry in an incident history (2026-08-11)
- WP-3.6: chose not to link the ticket to its CI here, although the event carries the CI id, because WP-3.7 owns "alert/ticket carries CMDB context ... link to CI" and SESSION.md §3.1's default is to record it and stay in scope (2026-08-11)
- WP-3.6: chose the ticket title to drop the check name when the summary already opens with it, because WP-3.5's summaries begin with the check name and prefixing unconditionally produced "[Critical] SNMP: CPU: SNMP: CPU: …" on the live estate (2026-08-11, found by hand-verification)
- WP-3.6: chose to prove the guard's Redis-failure fallback in process, against a multiplexer aimed at a closed port, rather than through the live loop, because hand-verification showed no alert reaches the automation during a Redis outage at all — WP-3.5's engine stalls first (2026-08-11)
- WP-3.7: chose to widen the existing `ICiDirectory`/`ITicketLinkDirectory` ports rather than add a third port or let Monitoring read the assets schema, because "alert carries CMDB context" is the same read Helpdesk already does for a linked asset and a second way of asking would drift from the first (2026-08-11)
- WP-3.7: chose to compute warranty status inside `CiDirectory` through `ContractExpiryCalculator` rather than return a raw date for each caller to judge, so "expiring soon" is the same thirty days on a ticket, on an alert and in a renewal notice (2026-08-11)
- WP-3.7: chose to read owner, location, warranty and open related tickets live on every request and snapshot none of them, following WP-2.4's ticket-link rule, because a CI cannot leave the module and a reassignment or a renewal has to reach every ticket and alert at once (2026-08-11)
- WP-3.7: chose the one exception to be the automated ticket's *description* and the alert's *audit entry*, which are written once and never rewritten, because both are dated records of what monitoring saw — the live view is the linked-asset card beside them (2026-08-11)
- WP-3.7: chose "open related tickets" to mean anything not Resolved or Closed, capped at five and never including the ticket being viewed, because a card that cited itself as prior art is noise and an uncapped list turns one card into a second ticket queue (2026-08-11)
- WP-3.7: chose the automation to link the CI through `ITicketCiLinkService` under the system actor rather than writing the link row, so the automated link is audited, publishes `TicketCiLinked` and is identical to the one an agent's "Link asset" button makes — which is what makes it visible from both sides (2026-08-11)
- WP-3.7: chose a failed or impossible CI link never to fail the raise, because the ticket is the thing somebody has to act on and a missing link is a degraded ticket rather than a lost alert (2026-08-11)
- WP-3.7: chose a CI that is absent from the CMDB to be stated in words ("not found in the CMDB") rather than left as empty fields, because "Owner: —" on an unfindable CI reads as an unowned asset, which is a different fact (2026-08-11)
- WP-3.7: chose to put the alert-side enrichment in an `IAlertEnrichmentService` that the engine calls on publication only, over enriching during evaluation, because publications happen on a severity change rather than per cycle — the two port reads are off the hot path (2026-08-11)
- WP-3.7: chose not to add an `/api/alerts` read surface for the alert board, leaving it to WP-3.9 as WP-3.5 already recorded, and to make the alert's CMDB context observable through the raise/clear audit entry and log line instead, because an endpoint written a package early is a guess about what the board needs (2026-08-11)
- WP-3.7: chose to give the three seeded DC1 core network CIs an owner (`technician2`), which the WP text did not call for, because the WP's own verification is "alert on seeded switch shows owner/location" and every seeded network device was previously unheld — a demo where the field reads "nobody holds this asset" proves the wiring and nothing else (2026-08-11)
- WP-3.7: no decision was changed by hand-verification (2026-08-11) — the checklist passed as written; the live walk confirmed the live-versus-dated split reads correctly on a real ticket, where the description named one open related ticket and the card beside it named two
- WP-3.8: chose a third check type `Tls` over a `certExpiry` flag on the HTTP check, because a check carries one warning/critical pair and days-remaining falls while latency rises — one check cannot hold both meanings, and `AlertRules.PrimaryMetric` has to return one metric per check (2026-08-11)
- WP-3.8: chose the TLS check **not** to verify the certificate it reads (`CERT_NONE`, `getpeercert(binary_form=True)`), because a self-signed or already-expired certificate is exactly the one somebody wants a countdown for and a verifying handshake fails before it can produce a number — the check answers "when does this expire", never "is this trusted" (2026-08-11)
- WP-3.8: chose an expired certificate to be a **successful** measurement of a negative day count, following WP-3.3's rule that 100% packet loss is a measurement rather than a failure, because a failed check advances the availability rule instead and the days figure the threshold is set on would never reach the alert engine (2026-08-11)
- WP-3.8: chose `cryptography` (Apache-2.0/BSD) to parse the certificate, because the standard library returns a parsed certificate only for a *verified* handshake and hand-rolling X.509 ASN.1 in the code that decides whether a certificate is about to expire is the trade WP-2.7 already refused for QR error correction (2026-08-11)
- WP-3.8: chose the TLS check to publish numbers only — no issuer or subject text — because WP-3.4 keys a text metric as a fact on (device, metric name), so two TLS checks on one device would overwrite each other and leave a field naming the wrong certificate (2026-08-11)
- WP-3.8: chose `expectedStatus` to be one status code rather than a list or a `2xx` class, because the poller has to apply the rule identically in another language and every form the rule can take is a form the two can come to disagree about — the trap `AlertRules.PrimaryMetric` already carries (2026-08-11)
- WP-3.8: chose HTTP checks to be limited to GET and HEAD, and `expectedContent` on a HEAD request to be refused at the API (400), because a check reads a service rather than changing one and a body expectation against a bodiless response can never pass (2026-08-11)
- WP-3.8: chose not to follow redirects unless `followRedirects` says so, because a check that follows a 302 to a login page reports on a page nobody asked about and the redirect is usually the fact worth alerting on (2026-08-11)
- WP-3.8: chose to cap the HTTP body read at 1 MiB and match `expectedContent` against what was read, over reading the whole response, because a monitoring agent must not pull a gigabyte because somebody pointed a check at a download (2026-08-11)
- WP-3.8: chose a TCP check to connect and close without sending a probe or reading a banner, because a connect proves a listener is accepting — which is the whole question — and a banner read would make the check protocol-specific, which is what the HTTP check next door is (2026-08-11)
- WP-3.8: chose a fourth seeded device on a *service* CI, pointed at MailHog by container name, over hanging the service checks off one of the three seeded network devices, because a TCP check's target is the device's own address and the seeded network devices point at the SNMP simulator (2026-08-11)
- WP-3.8: chose the seeded service device to carry **no** ICMP check, so that a device whose reachability is decided entirely by service checks (WP-3.3's "any check that completed" fallback) is part of the seeded estate rather than a shape nothing has run (2026-08-11)
- WP-3.8: chose not to seed a TLS check at all, because nothing in the dev stack serves HTTPS and a seeded check against a public host would raise a permanent alert on a machine with no internet; the checklist creates one by hand instead (2026-08-11)
- WP-3.8: hand-verification (2026-08-11) changed no decision — all seven checklist steps passed, and the TLS day count was cross-checked against `openssl s_client` to within the 50 seconds between the two measurements; the one defect found (`check.latency_ms` is event-loop wall time, inflated 60× by pysnmp blocking the loop) is WP-3.3 code and is recorded in STATUS.md for its own `fix/` branch rather than fixed here (2026-08-11)
- WP-3.8: chose the availability summary to terminate its sentence exactly once (`AlertRules.EndSentence`) rather than always append a full stop, because the poller reports whole sentences and WP-3.5 had been rendering "…after 3 packets.." on every failing check since it shipped; punctuation is left alone when present and added when absent, so an unpunctuated reason still cannot run into what follows it (2026-08-11, found by hand-verification)
- WP-3.8: chose the seeded service device to name its CI through `AssetsInfrastructureSeedResult.CiIds["svc-mail"]` over `ServiceCiIds.Take(1)`, because the positional pick was the finance reporting service and made the demo report a mail outage as a reporting one — and because a key survives the estate reordering that WP-2.8 warns renumbers every positional id (2026-08-11, found by hand-verification)
- WP-3.9: chose to broadcast to the dashboards directly from the alert engine and the acknowledge endpoint rather than from a consumer of `AlertRaised`/`AlertCleared`, because the outbox delivers on its own sweep and a live board that lagged a durable queue would not be live; the push is a projection of state already committed, nothing downstream depends on it, and a browser that misses one re-reads everything on reconnect (2026-08-11)
- WP-3.9: chose `IMonitoringBroadcaster` as a port in `Modules.Monitoring` implemented by `Web.Host` over the hub, following the `Platform/Integration` shape one direction further out, because ARCHITECTURE §2 puts every SignalR hub in the host and a module may not reference it (2026-08-11)
- WP-3.9: chose the hub to expose no client-callable methods at all, so acknowledging is only ever the HTTP endpoint that validates, authorizes and audits, rather than a second write path around all three (2026-08-11)
- WP-3.9: chose acknowledgement to be an annotation that suppresses nothing — it does not reach the state machine, does not change severity, and does not travel to WP-3.6's ticket automation — because the alert row states what is true about the estate and "a human has seen it" must not be able to change that (2026-08-11)
- WP-3.9: chose an acknowledgement not to survive a clear, because WP-3.5 opens a new row for a recurrence rather than reviving the old one, so the same problem coming back is unacknowledged again — which is the point of the button (2026-08-11)
- WP-3.9: chose acknowledging an already-acknowledged or an already-cleared alert to be a 409 that names who holds it, over an idempotent no-op, because the second press is somebody who could not see that a colleague already owned it and silently overwriting the first name hides exactly the fact they needed (2026-08-11)
- WP-3.9: chose to snapshot the acknowledger's display name beside their immutable id, following the WP-1.7 comment-author precedent, because a board has to print a name without asking an identity provider per row and has to keep printing one after that person leaves the directory (2026-08-11)
- WP-3.9: chose the alert list to carry the batched CI half of the WP-3.7 context and the alert *detail* to carry open related tickets as well, because `ICiDirectory` takes a list of ids while `ITicketLinkDirectory` answers one CI at a time — including tickets on the list would be a query per row on every board refresh (2026-08-11)
- WP-3.9: chose a device tile's status to be its worst open alert's severity, with `Unknown` for an enabled device that has never reported and `Disabled` for one switched off, because "Ok" on a device nobody has heard from is the same lie as printing 0 for a count that failed to load (WP-2.11's rule) (2026-08-11)
- WP-3.9: chose a disabled device's stale open alerts to stay counted on its tile, because disabling a device stops the telemetry that would clear them, so they sit open forever and hiding them would make a stale alert invisible (2026-08-11)
- WP-3.9: chose to order alerts worst-first with two boolean sort keys rather than by the severity column, because severity is stored as its name and Postgres would sort Critical, Ok, Warning alphabetically and bury the warnings under the recoveries (2026-08-11)
- WP-3.9: chose both boards' headline counts to be counted server-side over the whole estate rather than over the page, because a headline figure that changed when somebody turned a page is one nobody could quote (2026-08-11)
- WP-3.9: chose a push to patch the one row or tile it names rather than trigger a refetch, and a reconnect to re-read everything, because a board that refetched per alert would flicker on a busy estate while a board that never re-read would drift silently after a dropped socket (2026-08-11)
- WP-3.9: chose to accept the access token from the query string for `/hubs` paths only, because a browser cannot set an Authorization header on a WebSocket handshake and a token in a URL is a token in an access log — so it is scoped to the two paths that need it (2026-08-11)
- WP-3.9: chose to lazy-load Recharts behind the device page alone, because it is a third of the bundle and no other screen draws a chart (2026-08-11)
- WP-3.9: chose the two boards to share one nav item with in-page tabs rather than two sidebar entries, because they answer the same question from two directions and a second entry would leave the shell's Monitoring item meaning nothing (2026-08-11)
- WP-3.10: chose routing rules to be *additive* — every matching rule fires, deduped by channel — over a first-match-wins ordering, because "Critical to chat and email, Warning to email only" is then two independent rules rather than a priority list somebody has to keep correct (2026-08-11)
- WP-3.10: chose a collision between two rules on one channel to resolve toward the operator hearing about it (a rule that would send beats one that would withhold; one that digests beats one that drops), because the alternative is a notification lost to a configuration nobody wrote on purpose (2026-08-11)
- WP-3.10: chose to put the alert→notification consumer in `Modules.Monitoring` rather than in Platform, because a rule can be scoped to a device group and the poller group is a monitoring fact — the group travels on the envelope so Platform never reads a monitoring table (2026-08-11)
- WP-3.10: chose a Platform-owned `NotificationSeverity` (Informational/Warning/Critical) translated from `AlertSeverity` at the module boundary, over sharing an enum, because `AlertSeverity` belongs to Monitoring and Platform may not reference a module (2026-08-11)
- WP-3.10: chose a cleared alert to notify at Informational rather than at the severity it was, so a chat rule set to "Critical only" stays a pager instead of also becoming the all-clear (2026-08-11)
- WP-3.10: chose routing rules to select *channels* and per-user preferences to govern only notifications addressed to a person, because a team's channel is nobody's to mute while a technician's own mail is theirs (2026-08-11)
- WP-3.10: chose a user with no preference row to receive everything by email, matching the behaviour of every package before this one, because a technician who has never opened the preferences screen must still be told their SLA is about to breach (2026-08-11)
- WP-3.10: chose a webhook URL to be write-only — redacted in every response and in the audit entry, and an omitted `target` on update meaning "leave it alone" — because anyone holding an incoming-webhook URL can post into the channel, which is ARCHITECTURE §7's vault rule applied a package early (2026-08-11)
- WP-3.10: chose one `notification_deliveries` table to be both the durable record and the digest queue (a withheld notification is a Deferred row carrying its release instant), over a separate queue table, because two tables would have had to agree about the same fact (2026-08-11)
- WP-3.10: chose to write the delivery row *after* the attempt rather than before it, breaking with WP-2.6's contract-notice rule, because this row states an outcome and the path is already protected from replay by the consumer's idempotency key rather than by the row's existence (2026-08-11)
- WP-3.10: chose a failed digest to leave its contributors Deferred so the next pass retries them, because a duplicate digest is a better failure than a silent hole (2026-08-11)
- WP-3.10: chose quiet hours to need both ends or neither, refusing a half-stated window, because a platform that guessed the other end would silence notifications nobody asked to silence (2026-08-11)
- WP-3.10: chose an unknown time zone to fall back to UTC at evaluation time while being refused at write time, because a typo in operator-entered configuration must not be able to throw on the alerting path (2026-08-11)
- WP-3.10: chose the legacy Teams MessageCard over an Adaptive Card because it needs no schema-version negotiation and its `potentialAction` is what renders the deep link as a button rather than a bare URL (2026-08-11)
- WP-3.10: chose to build both webhook payloads by hand with `System.Text.Json` rather than take a Teams or Slack SDK, because each is one JSON document posted to one URL and a dependency would be larger than the code it replaced (2026-08-11)
- WP-3.10: chose `SlaService` to call the router in place of `INotificationService` rather than add consumers for `SlaWarningRaised`/`SlaBreached`, because the service already holds the assignee the notification is addressed to and a consumer beside the existing direct send would have doubled every warning (2026-08-11)
- WP-3.10: chose an SLA breach to notify at Critical, which it never did before, because until now only the silent reassignment said a breach had happened (2026-08-11)
- WP-3.10: chose an envelope with no device group to *not* match a rule that names one, because an SLA breach is not about a device and "no group" must not read as "every group" (2026-08-11)
- WP-3.10: chose the deep link to be `{base}/monitoring/alerts?alertId=…` opening the existing board's new detail drawer, over a dedicated alert page, because `GET /api/alerts/{id}` was built in WP-3.7 for exactly this caller and a drawer is DESIGN.md §6's detail-peek (2026-08-11)
- WP-3.10: chose to seed the email channel and its two rules unconditionally but the chat channel only when a webhook URL is configured, because a placeholder webhook would fail every Critical alert and read as a broken feature — the same call WP-3.8 made about not seeding a TLS check (2026-08-11)
- WP-3.10: chose channels and routing rules to sit behind `AdminOnly` while a person's own preference needs only a sign-in, because a rule decides who is woken at three in the morning and a preference decides only what its owner hears (2026-08-11)
- WP-3.10: chose the notification dedupe key to be `alert:{deviceId}:{ruleId}:{raised|cleared}` rather than the alert row's id, because an alert row is new on every recurrence and a per-row key meant a digest listed the same failing rule once per occurrence instead of collapsing it — which is the whole value of a digest (2026-08-11, found by hand-verification)
- WP-3.10: chose an alert's notification subject to be `[{severity}] {summary}` with the check carried as a fact, dropping the check-name prefix, because the poller writes whole sentences that already name the check and the prefix produced "Reachability: Reachability on snmpsim is failing…" on every message in the estate (2026-08-11, found by hand-verification)
- WP-3.10: chose the digest to strip a leading bracketed prefix from a subject before stating severity itself, over trusting the subject or dropping its own prefix, because alert subjects carry severity and SLA subjects do not (2026-08-11, found by hand-verification)
- WP-3.11: chose ASP.NET Data Protection with the key ring persisted to `platform.data_protection_keys` over the default filesystem ring, because the default mints a fresh key when a container restarts and every stored credential silently becomes undecryptable — the ring and the ciphertext now travel together through a restart, a redeploy and a database restore (2026-08-12)
- WP-3.11: chose to hand-write the ~40-line EF `IXmlRepository` rather than take `Microsoft.AspNetCore.DataProtection.EntityFrameworkCore`, because the interface is two methods and the official package requires the DbContext to implement its own interface, which would put a Data Protection concept into the shared platform context (2026-08-12)
- WP-3.11: chose to leave the key ring itself unencrypted at rest and to say so, rather than protect it with a certificate committed to this repository, because on a single host the key and the ciphertext are equally readable to anyone with the database and a real answer needs a KMS or an HSM — which is a deployment decision, not a code one (2026-08-12)
- WP-3.11: chose the Data Protection purpose to carry its own version (`Platform.Vault.CredentialMaterial.v1`), because the purpose string is part of the key derivation, so a future format change gets a `.v2` and a migration rather than an edit that makes every existing ciphertext unreadable (2026-08-12)
- WP-3.11: chose the secret to be one protected JSON document of per-kind fields over a column per field, because the fields differ by kind and a nullable column per possible field would be a schema that states which kinds exist twice (2026-08-12)
- WP-3.11: chose `CredentialRules` as a pure per-kind validator beside FluentValidation, following WP-3.1's `CheckRules`, because which fields a secret must carry is a relationship between the kind and the material that a per-property rule cannot see — and because these rules are the last thing between a typo and a check that authenticates with nothing (2026-08-12)
- WP-3.11: chose every message the vault produces to name a field and never quote its value, because a validation error reaches a log and a browser's network tab (2026-08-12)
- WP-3.11: chose material to leave the platform only through a **short-lived single-use grant**, over letting the poller's bearer token fetch secrets directly, because a stolen poller token then buys one two-minute window over one poller group's credentials rather than the whole vault (2026-08-12)
- WP-3.11: chose to split the poller's credential path into a cheap metadata **scope** (`GET /api/pollers/{name}/credentials`, no material, no row) and a **grant** (`POST .../credential-grants`, a row and an audited release), because a poller that redeemed every cycle would mint a grant row and write an audit entry every fifteen seconds per credential forever, while a poller that never re-asked would not notice a rotation (2026-08-12)
- WP-3.11: chose a credential's **version** as the rotation signal, over pushing anything to the poller or bumping the monitoring config version, because Platform may not write to a monitoring table and the poller already asks the platform something every cycle — the number moves, the next cycle notices, and the material travels only then (2026-08-12)
- WP-3.11: chose the grant's scope to be derived by **Monitoring** from the poller's own enabled checks on enabled devices in its group, and the poller's request to name nothing at all, because "which credentials is this poller entitled to" is a monitoring question and a request that named ids would be a request a poller could widen (2026-08-12)
- WP-3.11: chose the grant token to be 256 bits from the cryptographic RNG, returned once and stored only as a SHA-256 hash, because a stolen database should hand somebody grant rows they cannot redeem; a plain hash rather than a password KDF because the input is machine-generated entropy with no dictionary to slow down (2026-08-12)
- WP-3.11: chose redemption to look the grant up **by token hash** and then check the supplied id against what it found, and to answer 404 identically for an unknown grant, a wrong token and a mismatched pair, because distinguishing them turns the endpoint into an oracle (2026-08-12)
- WP-3.11: chose to sweep expired grants on every issue, over leaving them, because the row is a two-minute permission slip nobody reads afterwards — this is deliberately the opposite call to WP-3.10's `notification_deliveries`, which is a durable record and is the retention problem no WP owns (2026-08-12)
- WP-3.11: chose `AdminOnly` for managing credentials rather than `CanManageMonitoring`, because a technician who can edit a check must not thereby be able to replace the secret it authenticates with (2026-08-12)
- WP-3.11: chose redemption to sit behind `CanPoll` — the Poller realm role and **not** Admin, following WP-3.2's disjointness rule — so there is no operator-facing path to material anywhere in the platform, not even an administrator's (2026-08-12)
- WP-3.11: chose one `Accessed` audit entry **per credential** rather than one per redemption, because "who has read this credential and when" is then a query on entity id, which is the question somebody asks after a credential is suspected of having leaked (2026-08-12)
- WP-3.11: chose to audit grant **issue** as well as redemption, because a grant minted and never spent is the signature of somebody probing the endpoint and leaves no trace in the access trail otherwise (2026-08-12)
- WP-3.11: chose `ICredentialUsageDirectory` as a new port in `Platform/Integration` implemented by Monitoring — the first port Platform itself is the caller of — because the vault's delete guard has to ask whether a check still names a credential and Platform may not query `monitoring.check_definitions` (2026-08-12)
- WP-3.11: chose a credential in use to refuse deletion with 409 and to offer deactivation instead, following WP-2.3's relationship guard, because deleting it would leave the check authenticating with nothing from the next cycle and the only symptom would be a check that stopped working (2026-08-12)
- WP-3.11: chose the delete guard to count **every** check naming a credential, enabled or not, because a disabled check is one somebody intends to switch back on (2026-08-12)
- WP-3.11: chose a check's `credentialId` to be a complete statement on update, like WP-3.5's alert-tuning block, because omitting it is the only way to express "this check stops authenticating" (2026-08-12)
- WP-3.11: chose to refuse a credential whose kind the check type cannot use (400), because an SSH credential on an SNMP check would be stored happily, released happily, and produce a check that times out for a reason nothing states (2026-08-12)
- WP-3.11: chose to keep the plaintext-parameter path working for a check with no credential, rather than migrate every check and tighten `CheckRules`, because WP-3.1's unfixed `RequiredParameter[Snmp]` defect means tightening check validation here would break editing the seeded checks further (2026-08-12)
- WP-3.11: chose the seeder to **omit** the plaintext `community` entirely when a credential is supplied, rather than leave a copy beside it, because a stale copy would keep polling after a rotation and make the vault look like it was working while it was being bypassed (2026-08-12)
- WP-3.11: chose the poller to merge released material **over** a check's stored parameters at the moment the check runs, rather than into its cached configuration, because the vault's copy must outrank a stale plaintext one and a secret should exist in one place in the process (2026-08-12)
- WP-3.11: chose a missing credential to leave the check running unauthenticated with one warning per credential, over skipping the check, because a failed check with a reason is legible on a board while a check that silently stops running is not (2026-08-12)
- WP-3.11: chose `CredentialStore.retain` to drop material for any credential outside the platform's reported scope on every cycle, so a check that stops authenticating or a device that moves poller groups takes the secret out of the process rather than leaving it resident for as long as the container runs (2026-08-12)
- WP-3.11: chose to override `HeldCredential.__repr__`, because a dataclass repr prints every field and a traceback or a stray debug log would otherwise put a community string into a log file (2026-08-12)
- WP-3.11: chose a vault that cannot be reached to leave the material already held in force, following WP-3.2's rule that the poller swallows every dependency failure, because a poller that dropped its credentials over one timed-out request would unauthenticate an estate that is still fine (2026-08-12)
- WP-3.11: chose a credential whose ciphertext cannot be decrypted to be skipped with the id logged and no detail, rather than to throw, because a lost key ring must degrade to "this check polls unauthenticated" and not to a 500 on the poller's own configuration path — and a cryptographic failure's detail is a hint about the key ring (2026-08-12)
- WP-3.11: chose the credential's site scope to be metadata rather than an enforcement point, because a monitored device carries no site of its own (the CI does, read live through a port) and enforcing it would need a fourth read the WP text does not call for (2026-08-12)
- WP-3.11: chose no browser surface for the vault, following `/api/notification-channels` and `/api/ci-custom-fields`, because the WP text names none and a credential form is a screen that wants designing rather than one that falls out of an API (2026-08-12)
- WP-3.11: hand-verification (2026-08-12) changed no decision — all eleven checklist steps passed, the two seeded communities were proved distinct over a live socket (9%/22.5% against 91%/95%), and a rotation reached the poller in 13 seconds and carried the alert loop with it; the only correction was to the checklist's own wording, which asked for a grep that the seeded credentials' names make meaningless (2026-08-12)
- WP-3.12: chose the E2E to drive `DeviceTelemetryReported` onto the real broker rather than to run the Python poller against snmpsim inside `dotnet test`, because the poller's own suite proves it measures a device and `PollerEnvelopeTests` proves the envelope it emits — putting a container image build inside the test would re-prove both and make the one test that has to stay green depend on a Docker build (2026-08-12)
- WP-3.12: chose one test for the whole loop (degrade → alert → ticket → recover → resolve) over a test per link, because the recovery half can only be asserted against a ticket the degradation half opened and splitting them means driving the first half twice (2026-08-12)
- WP-3.12: chose to publish one cycle at a time and wait for the rule's Redis state to record each reading before sending the next, because the "for N cycles" counter is a read-modify-write and MassTransit hands a consumer several messages from one queue at once — three cycles published back to back can interleave, both read a count of one, and the run to three never completes (2026-08-12)
- WP-3.12: chose the automated failure path to be a quiet check riding in the *same* telemetry batches as a breaching one, over publishing below-threshold readings and waiting to see nothing, because by the time the breaching rule has raised the quiet rule has provably been evaluated against the same messages — a fixed wait passes on a slow machine for the wrong reason (2026-08-12)
- WP-3.12: chose the down-able device to be a second snmpsim container rather than a third community on the existing one, because stopping the shared simulator takes the healthy and degraded devices with it and the Phase 3 demo is "take *one* device away" (2026-08-12)
- WP-3.12: chose the down-able simulator to serve its profile under the `healthy` community — snmpsim takes the community from the file name — so the device authenticates with the credential WP-3.11 already seeds instead of adding a third secret to the vault; the container decides which device answers, the community decides which profile within it (2026-08-12)
- WP-3.12: rejected snmpsim's `delay` variation module for the down-able profile after probing the image: it blocks the whole responder, so one delayed community stalls every other one (a healthy read taken during a 15s delay timed out at 5.15s) — and rejected the `error` module because it answers immediately, which is a device returning an error rather than a device that is not there (2026-08-12)
- WP-3.12: chose the mock HTTP target to be nginx serving a page in this repository over pointing more checks at MailHog, because a content expectation set against somebody else's UI rots on their next version bump — which is why WP-3.8's seeded HTTP check deliberately carries none, and why the mail device keeps it that way (2026-08-12)
- WP-3.12: chose to append the two new seeded devices after the existing four, with their CIs looked up by key (`dc1-acc-sw-01`, `svc-portal`), because the first four have fixed ids and WP-3.7 gave exactly their CIs owners — a positional insert would re-attribute every alert already recorded against them (2026-08-12)
- WP-3.12: chose the down-able device's checks to run on a 30-second interval against the other SNMP devices' 60, because three sustained cycles is the platform default and the demo is otherwise a four-minute silence between stopping a container and seeing a ticket (2026-08-12)
- WP-3.12: chose to add nothing to the CI workflow, because `dotnet test` already runs the E2E and ubuntu-latest already has the Docker the shared Testcontainers fixture needs — a separate E2E job would run the same test twice (2026-08-12)
- WP-3.12: chose to give `dc1-acc-sw-01` an owner (`technician2`), extending WP-3.7's call to the fourth monitored CI, because WP-3.12 makes it the down-able device — so its ticket is the one the Phase 3 demo puts on screen, and it read "Owner: nobody holds this asset" (2026-08-12, found by hand-verification)
- WP-3.12: chose the E2E's host to be disposed in `DisposeAsync`, unlike every other WebApplicationFactory in this project, because it is the only one running the real bus *and* a fast outbox sweep — left alive it kept delivering and deleting rows out of the shared Platform outbox that a later test class reads as its own evidence, failing `AlertEngineIntegrationTests.Evaluate_SustainedBreach_...` from another package's code minutes after this class finished (2026-08-12, found by hand-verification)
- WP-3.12: hand-verification (2026-08-12) confirmed the rig on two live `aspire run`s — all eight checklist steps pass; stopping the down-able container alerted its device in ~90s and left the shared simulator's devices untouched, the content expectation broke and healed with no restart, and three auto-tickets opened and auto-resolved; the two defects above were found and fixed and the second run re-verified the owner reaching the ticket (2026-08-12)
- WP-4.1: chose a new `Discovery` realm role, an `it-platform-discovery` service-account client and a `CanDiscover` policy over reusing the poller's `Poller`/`CanPoll`, following WP-3.1's additive-policy precedent, because `CanPoll` reaches the credential vault's redemption endpoint and a scanner has no devices and no credential scope — so a stolen scanner token must buy nothing the vault protects (2026-08-13)
- WP-4.1: chose the discovery service's SNMP communities to come from its own environment rather than from the WP-3.11 vault, because a scan meets devices that are not monitored yet: the vault's scope is derived from checks on known devices, so there is no check for a stranger's community to hang off and nothing for the vault to release — anything found this way is identified, never polled (2026-08-13)
- WP-4.1: chose `DeviceDiscovered` to carry no community string at all, even though the scanner knows which one answered, because the event travels the bus and lands in a review queue somebody reads (ARCHITECTURE §7.3); the scanner logs the community's *position* in its configured list instead, following WP-3.11's rule that the poller logs credential ids and never values (2026-08-13)
- WP-4.1: chose one `DeviceDiscovered` per device over a batch per scan, reversing WP-3.3's batching decision for telemetry, because a scan runs on a schedule measured in minutes and reports only the addresses that answered, while telemetry is the same devices every fifteen seconds — and WP-4.2's unit of work is one device, so a batch would be unpacked on arrival (2026-08-13)
- WP-4.1: chose a separate publish-only RabbitMQ account for the scanner (`RabbitMqDefinitions.DiscoveryExchanges`, one exchange) over adding a member to `PollerExchanges`, because a scanner that could publish telemetry could report a measurement of a device it has never polled, and a heartbeat would let it impersonate a poller outright (2026-08-13)
- WP-4.1: chose scan profiles to be sent whole on every fetch with no version and no delta, following WP-3.1's maintenance-window reasoning rather than its device-delta reasoning, because a group has a handful of profiles — a scanner that re-reads a short list costs nothing while one that misses a change scans the wrong range for an hour (2026-08-13)
- WP-4.1: chose the scanner's config fetch to be keyed by discovery *group* (`GET /api/discovery/{group}/scan-profiles`) rather than by a scanner name, because unlike a poller a discovery service registers nothing, holds no configuration version and sends no heartbeat — a name in the URL would be a string the server has never seen (2026-08-13)
- WP-4.1: chose no registration and no heartbeat for the discovery service, breaking with WP-3.2's poller, because a scanner with nothing new to report is indistinguishable from an estate with nothing new on it, which is the normal state of a scanned network; WP-4.2's review queue is what somebody actually watches (2026-08-13)
- WP-4.1: chose an unknown discovery group to be answered with an empty profile list rather than 404, because a scanner is deployed before anybody writes it a profile and 404 would make "nothing to scan yet" and "this platform has never heard of you" the same message on its first cycle (2026-08-13)
- WP-4.1: chose the range keyword `local` — the subnet the scanner is on, read from `/proc/net/route` and narrowed to at most a /24 around its own address — because Docker allocates the Aspire session network's subnet at session start, so a seeded literal CIDR would scan an address space nothing in the stack is on; the narrowing exists because a Docker /16 is 65,534 probes to find six containers, and it is logged with both numbers (2026-08-13)
- WP-4.1: chose to store a profile's ranges and ports as jsonb string/number arrays validated at the edge, following WP-3.1's check parameters, over a table of range rows, because the shapes a range may take belong to the scanner and a table of them would need a migration for every new form (2026-08-13)
- WP-4.1: chose the API to validate and *count* a range but never enumerate it, because a `10.0.0.0/8` posted to an endpoint that materialised addresses would be a sixteen-million-element list inside a web request; expansion happens in the scanner's own process (2026-08-13)
- WP-4.1: chose a per-range and per-profile ceiling of 65,536 addresses with the refusal naming both numbers, because the useful failure message for a mistyped prefix is the one that says what to type instead (2026-08-13)
- WP-4.1: chose the port fingerprint to run against *every* address in a range rather than only the ones that answered a ping, whenever the profile names ports, because a host that filters ICMP is otherwise never found — and to skip the step entirely when no ports are configured, so an ICMP-only sweep stays the cheapest useful scan (2026-08-13)
- WP-4.1: chose SNMP v2c only for the identify, because a scan tries a list of community strings against a stranger and USM needs a user, a protocol and a key per device — which is exactly the per-device configuration that only exists once a device is known (2026-08-13)
- WP-4.1: chose to try communities in order and stop at the first that answers, over firing them concurrently, because the list is short and a burst of simultaneous authentications at a stranger is what an SNMP brute-force looks like in an IDS log (2026-08-13)
- WP-4.1: chose to walk neighbours with the community the identify had already established rather than re-trying the list, and to skip the CDP walks entirely when `cdpCacheDeviceId` comes back empty, because that saves three round trips against every device with no CDP cache — which is most of them (2026-08-13)
- WP-4.1: chose LLDP and CDP neighbours to be reported additively, so a switch with both protocols on produces two rows for one link, because they carry different fields and reconciling them into one edge is WP-4.3's topology work (2026-08-13)
- WP-4.1: chose to wrap pysnmp in `asyncio.to_thread` in this service, unlike the poller, because WP-3.8's hand-verification measured a single SNMP check stalling the event loop for 35-81 ms — a poller runs a handful a cycle and merely mis-measures its own latency, while a scan identifies every address that answered and would serialise the whole sweep behind the parsing (2026-08-13)
- WP-4.1: chose to add LLDP rows to `src/AppHost/snmpsim/healthy.snmprec` (sorted first, because 1.0.8802 precedes 1.3.6.1 and snmpsim answers GETNEXT by walking the file in order), because no seeded profile carried a neighbour table and the LLDP/CDP half of this WP would otherwise be unverifiable on a live run (2026-08-13)
- WP-4.1: chose to seed two scan profiles — a `local` sweep and TEST-NET-1 `192.0.2.0/29` — so that both of the WP's verification cases happen on every fresh `aspire run` rather than needing to be created by hand, following WP-3.12's rule that a demo gesture should need no setup (2026-08-13)
- WP-4.1: chose to add discovery install/lint/test steps to `.github/workflows/ci.yml` rather than fold them into the poller's, because two `pip install -e` runs in one environment would hide a missing dependency in either; WP-3.12 added nothing to CI because `dotnet test` already ran its E2E, and this package adds a pytest suite nothing else would run (2026-08-13)
- WP-4.1: chose to drop Keycloak's named data volume from AppHost so the realm re-imports on every `aspire run`, following WP-1.9's clean-slate call for the Postgres volume, because `--import-realm` uses strategy IGNORE_EXISTING — with the realm persisted, every later edit to `it-platform-realm.json` was silently skipped and this package's own client did not exist on any machine that had run the stack before (2026-08-13, found by hand-verification)
- WP-4.1: chose to render an OID-valued var-bind numerically rather than through pysnmp's `prettyPrint`, because `prettyPrint` resolves it against whatever MIB modules happen to be installed beside the scanner — `sysObjectID` arrived as `SNMPv2-SMI::enterprises.8072.3.2.10` live while the fixture and the tests carried the dotted form, which would make WP-4.2's fingerprint match depend on the scanner's Python environment (2026-08-13, found by hand-verification)
- WP-4.2: chose to match on management IP, hostname/sysName and CI name rather than the MAC and serial the WP text names, because `DeviceDiscovered` carries neither — an IP sweep from another subnet cannot see a MAC (ARP is link-local) and SNMP's system group has no serial field — so the ladder is built from what a scan actually learns and the gap is stated rather than faked (2026-08-13)
- WP-4.2: chose "already monitored at this address" as the ladder's top rung, read through a new `IMonitoredAddressDirectory` port in `Platform/Integration` implemented by Monitoring, because a monitored device exists only because an operator created it and named the CI it is — that is a decision already taken, not an inference from a naming convention (2026-08-13)
- WP-4.2: chose the review queue to live in `Modules.Assets` rather than beside WP-4.1's scan profiles in Monitoring, because approving produces a CI and Assets owns CIs — putting it in Monitoring would make the synchronous half of an approval a cross-module write that could not answer with the id of what it created (2026-08-13)
- WP-4.2: chose the monitoring enrolment to cross the module boundary as a `DiscoveredDeviceApproved` event rather than a second method on the port, because ARCHITECTURE §3 says a port is a read surface and never a write path; the read direction is a port and the write direction is an event (2026-08-13)
- WP-4.2: chose to publish `DiscoveredDeviceApproved` on every approval including the ones that asked for no monitoring, with the intent carried as a flag on it, because "the CMDB gained a CI nobody typed" is a fact either way and WP-4.3's topology work is the next likely reader (2026-08-13)
- WP-4.2: chose a matched discovery to write `assets.ci_discovery_facts` and never the CI's own attributes, because a scan observes while an operator asserts — overwriting the recorded values would destroy exactly the difference WP-4.6's drift report exists to find (2026-08-13)
- WP-4.2: chose to keep the reported `sysDescription` verbatim as the WP's "firmware" rather than parse a version out of it, because no format is shared across vendors and a regex that guesses produces a field that is confidently wrong instead of plainly raw (2026-08-13)
- WP-4.2: chose one ledger row per discovered *identity*, upserted on each sighting, over one row per message, because a profile sweeping every five minutes reports the same estate every five minutes and a row per message would make the queue unreadable within an hour (2026-08-13)
- WP-4.2: chose the identity key to be tiered — sysName, else short reverse-DNS name, else address — but the ledger lookup to search *every* stored field and rewrite the key in place, because the tiers are not stable across a device gaining an SNMP agent and a key-only lookup would make that device a second card and silently break its ignore-list entry (2026-08-13)
- WP-4.2: chose an address match to be refused when both sides carry a sysName and the two disagree, because that is a DHCP lease moving rather than one device — merging them would rewrite one device's history, and its review decision, with another's (2026-08-13)
- WP-4.2: chose a rung that finds two CIs to stop the walk and queue the discovery as `Ambiguous` with both contenders on the card, rather than fall through to a weaker rung, because resolving a tie with a lesser signal answers a question nobody asked while the estate still contains two CIs claiming one address (2026-08-13)
- WP-4.2: chose a rejected row to be kept forever and consulted before any matching runs, because "reject → never reappears" is exactly that row surviving every later scan; the sighting counters keep moving so the ignore list still says the thing is out there (2026-08-13)
- WP-4.2: chose approval to route the new CI through `ICiService.CreateAsync` rather than the DbContext, following WP-2.5's importer rule, so a CI that arrived from a scan is validated, `CiTypeSchema`-bound, audited and published exactly like one typed into the form (2026-08-13)
- WP-4.2: chose an approval missing the attributes its type requires to be a 400 naming each field, over defaulting vendor and port count to "Unknown" so the button always works, because a scan genuinely does not observe them and a CMDB full of confident nonsense is worse than a form somebody has to finish (2026-08-13)
- WP-4.2: chose the approval form to pre-fill only attributes discovery actually observed — a management IP for a network device, a hostname for a server — and to mark them "From the scan", so the boundary between what was measured and what a human asserted is visible at the moment it is crossed (2026-08-13)
- WP-4.2: chose the suggested CI type to be `NetworkDevice` only when the device reported LLDP or CDP neighbours and `Hardware` otherwise, because nothing but network equipment runs those protocols while an open port 22 is answered by every switch in the estate (2026-08-13)
- WP-4.2: chose an auto-enrolled device to be created with one ICMP reachability check, because ICMP is the only check type needing no parameter and no credential (`CheckRules.RequiredParameter` names an OID, a port or a URL for every other) and a device with no checks is polled by nobody and appears on no board (2026-08-13)
- WP-4.2: chose auto-enrolment to be off by default on the approval form, because discovering something is not a decision to watch it and an approval that silently enrolled every printer would fill the alert board (2026-08-13)
- WP-4.2: chose a second approval or rejection of a settled card to be 409 rather than an overwrite, because approving twice would create a second CI for one device — the duplicate this whole package exists to prevent (2026-08-13)
- WP-4.2: chose the review queue to sit behind `CanManageAssets` and to be forbidden to the `Discovery` role, because a scanner that could approve its own findings would make the queue decorative — the other side of WP-4.1's disjointness rule (2026-08-13)
- WP-4.2: chose the consumer's dedupe key to be the event id rather than the device identity, because a redelivery must not count as a second sighting while the *next scan* reporting the same device is a new event that has to be processed — that is what moves last-seen (2026-08-13)
- WP-4.2: chose a settled row whose CI has since been deleted to fall back to Pending rather than keep pointing at it, because no foreign key spans a decision and a CI and a card naming a row that is gone is worse than one asking to be looked at again (2026-08-13)
